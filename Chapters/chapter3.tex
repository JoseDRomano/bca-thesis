% Section 3.1.1 - Missing data paragraph was trimmed to only report the observation 
% of missingness (which fields, how much), with a forward reference to Section 3.2.1 
% for the treatment. The philosophical justification for listwise deletion was moved out.

% Section 3.1.2 - Renamed the subsubsection from "Pre-processing and Normalisation" 
% to "Upstream Processing and Normalisation" to make clear this only describes what 
% TCGA/GDC did, not what you did. The matrix transposition and data leakage paragraphs 
% were removed entirely, replaced by a single forward-reference sentence pointing to Section 3.2.

% Section 3.2.1 - Handling Residual Missing Values now contains the philosophical 
% justification for listwise deletion (moved from 3.1.1), which reads naturally 
% here because the numbers and decisions are already in context. I also added a 
% placeholder citation \cite{sterne2009multiple} for the missing data handling 
% rationale - you should replace this with whatever missing data reference you're 
% using, or remove it if you prefer not to cite one.

% Section 3.2.2 is unchanged in content but is now the only place where data 
% leakage is discussed, eliminating the duplication.

\typeout{NT FILE chapter3.tex}
% ============================================================
%  Chapter 3 - Methodology and Pilot Study
% ============================================================

\chapter{Methodology and Pilot Study}
\label{ch:pilot}

Small introduction to the Chapter.

% ================================================================
\section{Data Acquisition and Characterisation}
\label{sec:data_acquisition}
% ================================================================

% ---------------------------------------------------------------
\subsection{Data Source}
\label{subsec:data_source}
% ---------------------------------------------------------------

The dataset used in this work originates from \textbf{The Cancer Genome Atlas
Breast Invasive Carcinoma} (TCGA-BRCA) project \cite{tcga_brca_gdc}, a large-scale, publicly available
multi-omics repository maintained by the National Cancer Institute. TCGA-BRCA
represents one of the most comprehensively characterized breast cancer cohorts in
the biomedical literature, providing standardised genomic, transcriptomic, and
clinical data for a large cohort of patients profiled with uniform experimental
protocols. Its public availability, scale, and annotation quality makes it the
canonical benchmark dataset for computational breast cancer subtyping studies,
and it has been used as the primary data source in the majority of related work
discussed in Chapter~\ref{chap:background}.

Two complementary data releases were obtained from the TCGA-BRCA portal and
used jointly throughout this work: a \textbf{microRNA expression matrix},
containing quantified expression levels for 888 miRNA features across 272
patient samples, and a \textbf{clinical data table}, containing 37 variables per
patient across a total of 463 records. These two sources are described in detail
in Sections~\ref{subsec:clinical_data} and~\ref{subsec:mirna_data} respectively.
Their integration into a unified modelling dataset is described as part of the
preprocessing pipeline in Section~\ref{sec:preprocessing}.

% ---------------------------------------------------------------
\subsection{The Working Cohort}
\label{subsec:working_cohort}
% ---------------------------------------------------------------

The full clinical release and the miRNA expression data contain records for 463 and 272 patients, respectively. However, not all
patients in the clinical table have a corresponding miRNA expression profile
available in the molecular data release, which will drastically decrease our 
working cohort given that our objective is to find meaningful associations between 
\gls{mirna}, (potentionally) clinical features and subtypes of \gls{bc}. 
After restricting the cohort to
individuals for whom matched miRNA expression profiles existed, a \textbf{working
cohort of 272 samples} was obtained with the intersection of the two data sources.
All 272 samples correspond to primary solid tumour tissue
(\texttt{SAMPLE\_TYPE = Primary}) from mostly female patients (with over 99\% representation)
, consistent with the epidemiology of breast cancer as a predominantly female disease.

\todo{Aceito Sugestões para mais dados interessantes que se possam inserir aqui}

\begin{table}[t]
    \centering
    \caption{Detailed Descriptive Statistics of the TCGA Dataset}
    \label{tab:detailed_statistics}
    \small
    \begin{tabular}{lc}
        \toprule
        \textbf{Statistic} & \textbf{Value} \\
        \midrule
        \textbf{General Metrics} & \\
        Total miRNA Samples ($N$) & 272 \\
        Total Clinical Samples & 463 \\
        Number of microRNAs & 888 \\
        Clinical Attributes & 37 \\
        \midrule
        \textbf{Clinical Data Insights} & \\
        Age Range & 26--90 years \\

        % \midrule
        % \textbf{Sample Distribution by Subtype} & \\
        % Basal-like & 41 \\
        % HER2-enriched & 29 \\
        % Luminal A & 103 \\
        % Luminal B & 71 \\
        \bottomrule
    \end{tabular}
\end{table}

The classification target throughout this work is the \textbf{PAM50 intrinsic
subtype}, a well-established gene-expression-based taxonomy that partitions
breast cancer into five biologically distinct classes: Luminal A, Luminal B,
HER2-enriched, Basal-like, and Normal-like (we have already explored the molecular 
and biological characteristics of each subtype in Section~\ref{sec:breast_cancer}). 
The subtype distribution within the working cohort is shown in 
Table~\ref{tab:pam50-distribution} and the accompanying Figure~\ref{fig:pam50-pie}.

\todo{A Figura está meio estranha, parece que lhe falta alguma coisa - um título ou algo do género}
% --- PAM50 distribution table + pie chart ---
\begin{figure}[h!]
    \centering
    \begin{minipage}{0.46\textwidth}
        \centering
        \captionof{table}{PAM50 subtype distribution in the working cohort
        ($N$\,=\,272).}
        \label{tab:pam50-distribution}
        \begin{tabular}{lrr}
            \hline
            \textbf{PAM50 Subtype} & \textbf{Count} & \textbf{\%} \\
            \hline
            Luminal A     & 117 & 43.0 \\
            Luminal B     &  75 & 27.6 \\
            Basal-like    &  45 & 16.5 \\
            HER2-enriched &  30 & 11.0 \\
            Normal-like   &   5 &  1.8 \\
            \hline
            \textbf{Total} & \textbf{272} & \textbf{100.0} \\
            \hline
        \end{tabular}
    \end{minipage}
    \hfill
    \begin{minipage}{0.50\textwidth}
        \centering
        \begin{tikzpicture}
            \pie[
                radius      = 2.6,
                color       = {
                    blue!50,
                    blue!25,
                    red!55,
                    orange!70,
                    gray!40
                },
                text        = legend,
                before number = \footnotesize,
                after number  = \%,
                sum           = 100
            ]{
                43.0/Luminal A,
                27.6/Luminal B,
                16.5/Basal-like,
                11.0/HER2-enriched,
                1.8/Normal-like
            }
        \end{tikzpicture}
    \end{minipage}
    \caption{PAM50 subtype distribution across the 272-sample working cohort.
    Luminal A is the dominant class (43\%), while Normal-like is severely
    underrepresented ($n = 5$; 1.8\%). This pronounced class imbalance is a
    central challenge addressed in the modelling strategy in
    Section~\ref{subsec:split}.}
    \label{fig:pam50-pie}
\end{figure}

The class distribution reveals a pronounced imbalance that is characteristic
of breast cancer populations in clinical and research settings. Luminal A is
the dominant subtype, comprising 117 of 272 patients (43.0\%), while
Normal-like is severely underrepresented with only 5 samples (1.8\%). This
asymmetry has direct consequences for model training and evaluation: a naive
classifier that always predicts Luminal A would achieve over 43\% accuracy
without learning anything about the underlying biology. The modelling decisions
described in Section~\ref{sec:supervised} are informed by this imbalance, and
stratified sampling is used throughout to ensure that each experimental fold
preserves the original class proportions.


% ---------------------------------------------------------------
\subsection{Clinical Data}
\label{subsec:clinical_data}
% ---------------------------------------------------------------

The clinical data table contains 37 variables per patient. Several of these
are administrative or redundant fields (e.g., \texttt{sampleId}, \texttt{patientId}, 
\texttt{studyId}, \texttt{CANCER\_TYPE}), 
that carry no predictive information for subtype classification and were excluded during
preprocessing. A (pre) feature selection process was applied to the clinical data, 
resulting in a subset of 20 variables. These were retained based on their 
perceived relevance and informative value for the predictive model. These features are listed in
Table~\ref{tab:clinical_features}, along with their data type, possible values,
and a brief description of what each variable represents clinically.

% --- Clinical features table ---
\begin{table}[p]
\centering
\caption{Clinical features retained for modelling, with data type, possible
values, and clinical meaning.}
\label{tab:clinical_features}
\small
\begin{tabular}{p{3.8cm} p{2.0cm} p{3.5cm} p{4.5cm}}
\hline
\textbf{Feature} & \textbf{Type} & \textbf{Values} & \textbf{Description} \\
\hline
\texttt{AGE}
    & Integer
    & 26--90
    & Patient age at diagnosis (years). \\

\texttt{ER\_STATUS}
    & Categorical
    & Positive, Negative, Indeterminate, Not Available
    & Oestrogen receptor status assessed by immunohistochemistry. \\

\texttt{PR\_STATUS}
    & Categorical
    & Positive, Negative, Indeterminate, Not Available
    & Progesterone receptor status assessed by immunohistochemistry. \\

\texttt{HER2\_STATUS}
    & Categorical
    & Positive, Negative, Equivocal
    & HER2 receptor amplification status. \\

\texttt{TUMOR\_STAGE}
    & Ordinal
    & T1, T2, T3, T4, TX
    & Primary tumour size/extent per TNM staging. \\

\texttt{TUMOR\_T1\_CODED}
    & Binary
    & T1, T\_Other
    & Binarised tumour stage: T1 vs.\ all others. \\

\texttt{NODES}
    & Ordinal
    & N0, N1, N2, N3
    & Regional lymph node involvement per TNM staging. \\

\texttt{NODE\_CODED}
    & Binary
    & Positive, Negative
    & Binarised lymph node involvement. \\

\texttt{METASTASIS}
    & Binary
    & M0, M1
    & Presence of distant metastasis per TNM staging. \\

\texttt{METASTASIS\_CODED}
    & Binary
    & Positive, Negative
    & Binarised metastasis indicator. \\

\texttt{CONVERTED\_STAGE}
    & Categorical
    & No\_Conversion, Stage I, Stage IIA, Stage IIB, Stage IIIA, Stage IIIB, Stage IIIC, Stage IV
    & Overall AJCC composite stage derived from TNM. \\

\texttt{FRACTION\_GENOME\_ALTERED}
    & Continuous
    & $[0,\,1]$
    & Proportion of the genome showing copy-number alterations. \\

\texttt{MUTATION\_COUNT}
    & Integer
    & $\geq 0$
    & Total number of somatic mutations detected. \\

\texttt{TMB\_NONSYNONYMOUS}
    & Continuous
    & $\geq 0$
    & Tumour mutational burden (non-synonymous mutations per Mb). \\

\texttt{OS\_MONTHS}
    & Continuous
    & $\geq 0$
    & Overall survival time from diagnosis to last follow-up (months). \\

\texttt{OS\_STATUS}
    & Binary
    & 0:LIVING, 1:DECEASED
    & Patient vital status at last follow-up. \\

\texttt{CN\_CLUSTER}
    & Integer (cluster)
    & 1--5
    & Copy-number cluster assignment from TCGA integrative analysis. \\

\texttt{METHYLATION\_CLUSTER}
    & Integer (cluster)
    & 1--5
    & DNA methylation cluster from TCGA integrative analysis. \\

\texttt{MIRNA\_CLUSTER}
    & Integer (cluster)
    & 1--7
    & miRNA expression cluster from TCGA integrative analysis. \\

\texttt{SIGCLUST\_UNSUPERVISED\_MRNA}
    & Integer
    & Signed integer scores
    & Significance of cluster assignment from unsupervised mRNA analysis. \\
\hline
\end{tabular}
\end{table}

The 20 retained features span a broad range of clinical and molecular
dimensions. The three receptor statuses, \texttt{ER\_STATUS},
\texttt{PR\_STATUS}, and \texttt{HER2\_STATUS}, are the canonical markers
used in clinical breast cancer diagnosis and are intrinsically aligned with the
PAM50 subtype labels: for example, the Basal-like subtype is largely composed
of triple-negative tumours (ER$^-$/PR$^-$/HER2$^-$), a well-documented
biological correspondence that provides a natural consistency check for
classifier outputs. The TNM staging variables (\texttt{TUMOR\_STAGE},
\texttt{NODES}, \texttt{METASTASIS}) encode the anatomical extent of the
disease at diagnosis, while the genomic variables (\texttt{MUTATION\_COUNT},
\texttt{TMB\_NONSYNONYMOUS}, \texttt{FRACTION\_GENOME\_ALTERED}) reflect
tumour mutational characteristics that may (or may not) correlate with subtype-specific
oncogenic programmes. The integrative cluster assignments
(\texttt{CN\_CLUSTER}, \texttt{METHYLATION\_CLUSTER}, \texttt{MIRNA\_CLUSTER})
are derived from TCGA's own multi-omics analyses and encode cross-modal
molecular patterns that may complement the raw miRNA expression signal.

\paragraph{Data Descriptive Statistics} A descriptive overview of selected clinical characteristics within the working
cohort is as follows. Patient ages range from 26 to 90 years, with a mean of
$59.3 \pm 13.3$ years (median = 60), consistent with the known epidemiology of
breast cancer as predominantly a post-menopausal disease. Oestrogen receptor
positivity was recorded in 77.2\% of patients ($n = 210$), progesterone receptor
positivity in 64.7\% ($n = 176$), and HER2 amplification in 13.6\% ($n = 37$).
These proportions are broadly representative of an unselected breast cancer
population. Tumour stage at diagnosis was dominated by T2 lesions (61.4\%,
$n = 167$), followed by T1 (19.5\%), T3 (12.9\%), and T4 (5.1\%). From a
survival perspective, 89.0\% of patients ($n = 242$) were alive at the time of
last follow-up, with 30 recorded deaths.

% \begin{figure}[h!]
% \centering

% \begin{tikzpicture}

% % ── LEFT PANEL: Receptor status stacked horizontal bars ──────────────────────
% \begin{axis}[
%     name        = receptor,
%     xbar stacked,
%     width       = 0.44\textwidth,
%     height      = 5.2cm,
%     bar width   = 14pt,
%     xmin = 0, xmax = 100,
%     xtick       = {0, 25, 50, 75, 100},
%     xticklabel  = {$\pgfmathprintnumber{\tick}\%$},
%     xticklabel style = {font=\small},
%     ytick       = {1, 2, 3},
%     yticklabels = {HER2, PR, ER},
%     yticklabel style = {font=\small\bfseries},
%     xlabel      = {Proportion of cohort (\%)},
%     xlabel style = {font=\small, yshift=-2pt},
%     title       = {(a)~Receptor Status},
%     title style = {font=\small\bfseries, yshift=4pt},
%     enlarge y limits = 0.45,
%     legend style = {
%         at          = {(0.5,-0.30)},
%         anchor      = north,
%         legend columns = 3,
%         font        = \footnotesize,
%         draw        = none,
%         column sep  = 6pt,
%     },
%     legend cell align = left,
%     tick style  = {draw=none},
%     axis line style = {draw=none},
%     ymajorgrids = false,
%     xmajorgrids = true,
%     grid style  = {gray!20, thin},
% ]

% % Positive (blue)
% \addplot[xbar, fill=blue!55, draw=none] coordinates {
%     (13.6, 1)   % HER2+
%     (64.7, 2)   % PR+
%     (77.2, 3)   % ER+
% };

% % Negative (red)
% \addplot[xbar, fill=red!45, draw=none] coordinates {
%     (84.9, 1)   % HER2-
%     (33.8, 2)   % PR-
%     (21.7, 3)   % ER-
% };

% % Other / Equivocal / Not available (grey)
% \addplot[xbar, fill=gray!35, draw=none] coordinates {
%     (1.5,  1)   % HER2 equivocal
%     (1.5,  2)   % PR other
%     (1.1,  3)   % ER other
% };

% \legend{Positive, Negative, Equivocal / N.A.}

% % Inline percentage labels — positive bars
% \node[font=\scriptsize, text=white, anchor=west] at (axis cs: 1,  1) {13.6\%};
% \node[font=\scriptsize, text=white, anchor=west] at (axis cs: 1,  2) {64.7\%};
% \node[font=\scriptsize, text=white, anchor=west] at (axis cs: 1,  3) {77.2\%};

% \end{axis}

% % ── RIGHT PANEL: Tumour stage grouped vertical bars ──────────────────────────
% \begin{axis}[
%     name        = stage,
%     at          = {(receptor.east)},
%     anchor      = west,
%     xshift      = 1.6cm,
%     ybar,
%     width       = 0.40\textwidth,
%     height      = 5.2cm,
%     bar width   = 18pt,
%     ymin = 0, ymax = 75,
%     ytick       = {0, 20, 40, 60},
%     yticklabel  = {$\pgfmathprintnumber{\tick}\%$},
%     yticklabel style = {font=\small},
%     xtick       = {1, 2, 3, 4, 5},
%     xticklabels = {T1, T2, T3, T4, TX},
%     xticklabel style = {font=\small\bfseries},
%     ylabel      = {Proportion of cohort (\%)},
%     ylabel style = {font=\small, yshift=2pt},
%     title       = {(b)~Tumour Stage},
%     title style = {font=\small\bfseries, yshift=4pt},
%     enlarge x limits = 0.18,
%     nodes near coords,
%     nodes near coords style = {font=\scriptsize, anchor=south, yshift=1pt},
%     nodes near coords align = {vertical},
%     tick style  = {draw=none},
%     axis line style = {draw=none},
%     xmajorgrids = false,
%     ymajorgrids = true,
%     grid style  = {gray!20, thin},
% ]

% \addplot[fill=blue!45, draw=none] coordinates {
%     (1, 19.5)   % T1
%     (2, 61.4)   % T2
%     (3, 12.9)   % T3
%     (4,  5.1)   % T4
%     (5,  1.1)   % TX
% };

% \end{axis}

% \end{tikzpicture}

% \caption{Clinical profile of the working cohort ($N$\,=\,272).
% \textit{(a)} Receptor status distribution for oestrogen receptor (ER),
% progesterone receptor (PR), and HER2 amplification, shown as stacked
% horizontal bars (positive, negative, equivocal/not available).
% \textit{(b)} Tumour stage distribution at diagnosis per TNM classification.
% T2 lesions dominate (61.4\%), and ER positivity is the most prevalent
% receptor finding (77.2\%). TX indicates cases where tumour extent could
% not be assessed.}
% \label{fig:clinical_profile}
% \end{figure}

Several clinical features contain missing values that require attention during
preprocessing. The integrative cluster columns
(\texttt{INTEGRATED\_CLUSTERS\_NO\_EXP},\allowbreak
\texttt{INTEGRATED\_CLUSTERS\_UNSUP\_EXP},\allowbreak
\texttt{INTEGRATED\_CLUSTERS\_WITH\_PAM50}) and \texttt{RPPA\_CLUSTER} are
missing for a large fraction of patients (approximately 76 and 73 records
respectively) and were therefore excluded from the modelling feature set prior
to the cohort restriction step. Among the 20 retained features, missing values
are sparse (at most 4 records for \texttt{METASTASIS} and
\texttt{METASTASIS\_CODED}). The strategy for handling these residual
missing values is described in Section~\ref{sec:preprocessing}.

% ---------------------------------------------------------------
\subsection{MicroRNA Expression Data}
\label{subsec:mirna_data}
% ---------------------------------------------------------------

The miRNA expression matrix contains quantified expression levels for
\textbf{888 miRNA features} measured across the 272 patient samples in the
working cohort. Each entry $x_{ij}$ represents the expression level of
miRNA $j$ in patient $i$, where higher values indicate greater expression
abundance in the tumour tissue. This constitutes a canonical
\textbf{high-dimensional, low-sample-size (HDLSS)} setting: the number of
features ($p = 888$) greatly exceeds the number of observations ($n = 272$),
a regime that poses well-known challenges for statistical learning and motivates
the feature selection strategies explored throughout this work. 

% This complexity 
% can be visually appreciated in the heatmap shown in Figure~\ref{fig:mirna-heatmap}, 
% which displays the expression patterns of a random subset of 50 patients and 50 miRNAs, 
% with the PAM50 subtype associated for context.

% [MEDIA: Heatmap of a random subset of patients $\times$ miRNAs (e.g.\ 50 patients
% $\times$ 50 miRNAs, sorted by subtype) to visually convey the high-dimensional
% nature of the expression matrix and give an intuition of the expression
% variation across subtypes. A colourbar from low (blue) to high (red) expression
% would work well here.]
% \begin{figure}[h!]
%     \centering
%     \includegraphics[width=\textwidth]{miRNA_heatmap_complexity.png}
%     \caption{miRNA expression heatmap for a random subset of 50 patients and the 50
%              highest-variance miRNAs from the TCGA breast cancer cohort. Rows are
%              grouped by PAM50 subtype (colour strip, left); columns are individual
%              miRNA features. Expression values are z-scored per miRNA; blue denotes
%              low and red denotes high expression. The block structure reflects
%              subtype-specific miRNA signatures.}
%     \label{fig:mirna-heatmap}
% \end{figure}

\subsubsection*{The miRNA Redundancy Problem}

A critical characteristic of the raw miRNA expression data, which becomes
apparent upon close examination, is the presence of widespread feature
redundancy. The 888 features in the raw matrix do not represent 888
biologically distinct microRNAs. Instead, a large number of entries share the
same \textit{base identifier} - for example, \texttt{MIR-758/758},
\texttt{MIR-758/3P}, and \texttt{MIR-758/5P} all derive from the same
precursor microRNA (MIR-758), differing only in the strand arm or isoform
designation. Across the full matrix, 888 Hugo gene symbols map to only 442
unique base identifiers, meaning that 446 features are variants of a base
miRNA already present in the data.

More importantly, upon inspection of the actual expression values, many of
these variants are found to carry \textbf{identical expression profiles} across
all 272 patients - that is, for a given patient, \texttt{MIR-758/758},
\texttt{MIR-758/3P}, and \texttt{MIR-758/5P} all report exactly the same
numerical value. This redundancy is attributable to a fundamental limitation of
the sequencing and quantification technology used to generate the TCGA-BRCA
expression data: the platform lacks the resolution to distinguish between
closely related isoforms of the same precursor miRNA, and consequently assigns
the same measured abundance to all of them. From a machine learning perspective,
these duplicated features add no discriminative information whatsoever (they
are mathematically identical columns in the feature matrix) and including
them in the model would artificially inflate the apparent feature space while
introducing collinearity that could distort feature importance estimates.

This observation directly motivates one of the first and most consequential
preprocessing decisions in the pipeline: the identification and removal of
duplicate expression profiles, described in detail in
Section~\ref{sec:preprocessing}. As reported in our published study
\ref{ann:EPIA_paper}, this deduplication step reduces the miRNA feature count from
888 to 442, eliminating 446 non-informative redundant features while retaining
the full unique expression signal. The resulting feature matrix is both more
compact and more interpretable, with each remaining miRNA representing a
genuinely distinct molecular measurement.

Now that we have characterized the data sources and the working cohort, 
we can proceed to the pre-processing pipeline that transforms the raw data 
into a clean, numerical feature matrix suitable for machine learning. The next 
section describes this pipeline in detail, including the handling of missing 
values, feature selection decisions, and the critical steps taken to avoid 
data leakage between training and test partitions.

% ================================================================
\section{Data Pre-processing Pipeline}
\label{sec:preprocessing}
% ================================================================

Before any learning algorithm was applied, the raw data sources described in
Section~\ref{sec:data_acquisition} underwent a structured sequence of
transformations designed to produce a clean, fully numerical, and
modelling-ready feature matrix. The pipeline was designed with two overriding
principles in mind: preserving the biological authenticity of the data at every
step, and strictly preventing any statistical information from the test set from
influencing the training process (\textbf{Data Leakage Prevention}). The full procedure is summarised
schematically in Figure~\ref{fig:pipeline_diagram}, and each step is described
in the subsections below.

% [MEDIA: Pipeline diagram showing the sequential stages as a flowchart.
% Boxes: (1) Raw miRNA matrix (888×272) → Transpose + align → (272×888) |
% (2) Clinical table (463×37) → Inner join → Integrated matrix (272×925) |
% (3) Deduplicate miRNAs → (272×479) |
% (4) Drop uninformative clinical cols → (272×464) |
% (5) Listwise deletion + Normal-like exclusion → (244×464) |
% (6) Categorical encoding → (244×464, all numeric) |
% (7) Stratified 80/20 split → Train (195) / Test (49) |
% (8) StandardScaler fitted on Train only → applied to both.
% Arrows should emphasise that step 8 happens AFTER the split.]
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.70\textwidth]{diagrama_pre_processamento.drawio.png}
    \caption{Schematic overview of the data pre-processing pipeline. The raw miRNA
             expression matrix and clinical data table are transformed through a
             series of steps including integration, deduplication, feature
             selection, and encoding, culminating in a stratified train-test split
             and final scaling. Each step is designed to preserve data integrity
             while preparing the dataset for robust machine learning modelling.}
    \label{fig:pipeline_diagram}
\end{figure}

% ---------------------------------------------------------------
\subsection{Data Integration}
\label{subsec:integration}
% ---------------------------------------------------------------

The two raw data sources arrived in structurally incompatible orientations. The
miRNA expression matrix followed the conventional bioinformatics layout:
features (miRNAs) as rows and patients as columns, yielding a matrix of
$888 \times 275$ (where 3 of the 275 columns are metadata fields:
\texttt{entrezGeneId}, \texttt{hugoGeneSymbol}, and \texttt{type}). The
clinical data table, by contrast, followed the standard tabular layout with
patients as rows and variables as columns ($463 \times 37$). Before any join
could be performed, the miRNA matrix had to be transformed to match this
convention.
The transformation proceeded in four steps:

\begin{itemize}
    \item[(i)] the metadata columns \texttt{entrezGeneId} and \texttt{type} were dropped, retaining only the gene symbol and expression values;
    \item[(ii)] the matrix was transposed, placing patients on the rows and miRNAs on the columns;
    \item[(iii)] the first row of the transposed matrix (which contained the miRNA identifiers) was promoted to serve as the column header;
    \item[(iv)] the row index was renamed to \texttt{sampleId} to establish a common join key with the clinical table.
\end{itemize}

The two tables were then merged via an \textbf{inner join} on \texttt{sampleId}.
The inner join naturally restricts the combined dataset to patients for whom
both miRNA expression data and clinical annotations are available
simultaneously, yielding an integrated matrix of \textbf{272 samples} and
$888 + 37 = 925$ features prior to any reduction step. The 191 clinical
records without matched miRNA profiles were discarded at this stage.

% ---------------------------------------------------------------
\subsection{miRNA Deduplication}
\label{subsec:mirna_dedup}
% ---------------------------------------------------------------

As described in Section~\ref{subsec:mirna_data}, the 888 miRNA features in
the raw matrix do not represent 888 biologically independent measurements.
A substantial fraction of entries share the same precursor base identifier,
for example, \texttt{MIR-758/758}, \texttt{MIR-758/3P}, and
\texttt{MIR-758/5P} (visually explained in Table~\ref{tab:mirna-variants}). These 
carry \textbf{identical numerical expression values} across all 272 samples.
This redundancy comes from the limited sensibility of the extraction technique 
used to collect these miRNA expression values which cannot distinguish between 
closely related isoforms of the same precursor, and consequently assigns the 
same measured abundance to all of them.

% [SUGGESTION: A small illustrative table showing three example base identifiers
% (e.g.\ MIR-758, MIR-744, MIR-766) and their variants, with a column showing
% that expression values are identical across all patients, would make this point
% very concrete for the reader without requiring them to inspect the raw data
% themselves.]

\begin{table}[t]
        \centering
        \caption{Example of miRNA identifier variants sharing identical expression values
                across patients. Each row corresponds to a distinct annotation of the same
                base identifier (\texttt{MIR-744}), yet all variants carry the exact same
                expression value for every patient - here illustrated for two example
                patient IDs (\texttt{TCGA-A1-A0SE-01} and \texttt{TCGA-A1-A0SH-01}).
                This redundancy across variant annotations motivates the filtering step
                applied prior to modelling.}
        \label{tab:mirna-variants}
        \small
        \begin{tabular}{llcc}
        \toprule
        \textbf{Hugo Gene Symbol} & \textbf{Type} & \textbf{TCGA-A1-A0SE-01} & \textbf{TCGA-A1-A0SH-01} \\
        \midrule
        \texttt{MIR-744/744*} & miRNA & 3.67519164 & 3.03321064 \\
        \texttt{MIR-744/744}  & miRNA & 3.67519164 & 3.03321064 \\
        \texttt{MIR-744/3P}   & miRNA & 3.67519164 & 3.03321064 \\
        \texttt{MIR-744/5P}   & miRNA & 3.67519164 & 3.03321064 \\
        \bottomrule
    \end{tabular}
\end{table}

From a machine learning standpoint, retaining these duplicate columns is not
merely wasteful: \textbf{it is actively harmful}. Identical features contribute zero
additional information to the model while artificially inflating the apparent
dimensionality of the feature space, exacerbating the curse of dimensionality
in an already data-sparse regime. They also introduce perfect multicollinearity,
which can distort coefficient estimates in linear models and destabilise
feature importance rankings in tree-based models.

To address this, all miRNA features sharing identical expression profiles
across all samples were identified and all but one representative were removed.
The deduplication was applied by grouping features according to their base
identifier and retaining only those with a unique expression vector. This step
reduced the miRNA feature count from \textbf{888 to 442}, eliminating 446
redundant features without discarding any unique biological signal.

% ---------------------------------------------------------------
\subsection{Removal of Uninformative Clinical Features}
\label{subsec:clinical_drop}
% ---------------------------------------------------------------

When we look at the 37 original clinical variables, 15 were removed prior to modelling on
the grounds summarised in Table~\ref{tab:dropped_features}. These columns
fell into four categories: 

\begin{itemize}
    \item redundant patient identifiers that carry no predictive signal (\texttt{patientId}); 
    \item zero-variance fields that take a single value across all samples and therefore provide no discriminative information (\texttt{studyId}, \texttt{CANCER\_TYPE}, \texttt{CANCER\_TYPE\_DETAILED}, \\ \texttt{ONCOTREE\_CODE}, \texttt{SAMPLE\_TYPE}, \texttt{SOMATIC\_STATUS}, \texttt{SAMPLE\_COUNT}, \texttt{SEX});
    \item high-missingness fields where the proportion of missing values rendered the feature unreliable (\texttt{RPPA\_CLUSTER} at ${\approx}24\%$ missing; 
    \item \texttt{INTEGRATED\_CLUSTERS\_NO\_EXP}, \texttt{INTEGRATED\_CLUSTERS\_UNSUP\_EXP}, and  \\ \texttt{INTEGRATED\_CLUSTERS\_WITH\_PAM50} each at ${\approx}25\%$ missing);
    \item task-irrelevant derived features (\texttt{SIGCLUST\_INTRINSIC\_MRNA}, a cluster assignment derived from mRNA rather than miRNA data, and \texttt{SURVIVAL\_DATA\_FORM}, a near-zero-variance administrative field).
\end{itemize}

\begin{table}[t]
\centering
\caption{Clinical columns removed during pre-processing, with the
rationale for each exclusion.}
\label{tab:dropped_features}
\small
\begin{tabular}{p{5.8cm} p{2.5cm} p{5.0cm}}
\hline
\textbf{Column} & \textbf{Reason} & \textbf{Detail} \\
\hline
\texttt{patientId}
    & Redundant identifier
    & Duplicate of \texttt{sampleId}. \\
\texttt{studyId}, \texttt{CANCER\_TYPE}, \texttt{CANCER\_TYPE\_DETAILED},
\texttt{ONCOTREE\_CODE}, \texttt{SAMPLE\_TYPE}, \texttt{SOMATIC\_STATUS},
\texttt{SAMPLE\_COUNT}, \texttt{SEX}
    & Zero variance
    & Single value across all 272 samples (all primary female BRCA). \\
\texttt{RPPA\_CLUSTER}
    & High missingness
    & ${\approx}24\%$ missing values. \\
\texttt{INTEGRATED\_CLUSTERS\_NO\_EXP}, \texttt{INTEGRATED\_CLUSTERS\_UNSUP\_EXP},
\texttt{INTEGRATED\_CLUSTERS\_WITH\_PAM50}
    & High missingness
    & ${\approx}25\%$ missing values each. \\
\texttt{SIGCLUST\_INTRINSIC\_MRNA}
    & Task-irrelevant
    & Derived from mRNA, not miRNA; circular with target. \\
\texttt{SURVIVAL\_DATA\_FORM}
    & Near-zero variance
    & Administrative field with negligible variation. \\
\hline
\end{tabular}
\end{table}

After these removals, 20 clinical features were retained alongside the 442
deduplicated miRNA expression features, for an intermediate feature matrix of
272 samples $\times$ 462 features (plus the \texttt{sampleId} identifier and
the \texttt{PAM50\_SUBTYPE} target).


% ---------------------------------------------------------------
\subsection{Handling Missing Values \& Low Representation Classes}
\label{subsec:missing_values}
% ---------------------------------------------------------------

Following the elimination of high-missingness columns, a small residual
number of samples still contained at least one missing value across the
remaining clinical features. Specifically, the affected fields and their
missing-value counts were: \texttt{FRACTION\_GENOME\_ALTERED} ($n = 2$),
\texttt{HER2\_STATUS} ($n = 1$), \texttt{METASTASIS\_CODED} ($n = 4$),
\texttt{TUMOR\_T1\_CODED} ($n = 3$), and \texttt{METASTASIS} ($n = 4$).
The miRNA expression matrix itself contained no missing values.

Deciding how to handle missing data is never a purely technical choice: the
appropriate strategy depends on the nature of the data, the mechanism
generating the missingness, and the downstream use. In computational studies
applied to purely numerical or synthetic data, some imputation methods such as
mean substitution, k-nearest neighbour imputation, or generative approaches
like MICE~\cite{buuren2011mice} are standard and often effective. In a
biological dataset such as this one, however, the situation is qualitatively
different. Clinical measurements such as metastasis status, HER2 assessment,
or genomic alteration fraction reflect real, heterogeneous biological processes
in individual patients. Filling in a missing HER2 status with the cohort mean,
or inferring a missing metastasis record from neighbouring samples, risks
fabricating a clinical picture that does not correspond to any real biological
state. Imputed values could mislead the model into learning artificial patterns
(in effect, training on synthetic patients that never existed). Given the
already limited size of the cohort, introducing even a small number of
biologically implausible observations could meaningfully distort the learned
decision boundaries.

Given that the number of affected samples is small relative to the overall cohort, 
and given the potential for imputed values to introduce bias, the affected samples 
were removed via \textbf{listwise deletion}. The miRNA expression matrix contained no missing values, so this
step exclusively affected rows with incomplete clinical annotations. In total,
28 samples were removed, \textbf{reducing the cohort from 272 to 251 samples.} This
represents a loss of 10.3\% of the working cohort, a non-trivial reduction,
but one that is justified by the imperative of data integrity in a biomedical
context. In addition, at the encoding stage described below, the 5 remaining
Normal-like samples were excluded from the supervised experiments due to their
insufficient representation for reliable learning and evaluation, bringing the
final modelling cohort to \textbf{244 samples} across four PAM50 subtypes:
Luminal A ($n = 103$), Luminal B ($n = 71$), Basal-like ($n = 41$), and
HER2-enriched ($n = 29$).
 
% ---------------------------------------------------------------
\subsection{Categorical Encoding}
\label{subsec:encoding}
% ---------------------------------------------------------------
Machine learning algorithms operate on numerical representations: they compute
distances, gradients, and split criteria over real-valued feature spaces.
Categorical variables, those that take a finite set of discrete named
values rather than continuous numbers, cannot be directly ingested by most
learning algorithms without first being converted to a numerical form. Among
the 20 retained clinical features, 11 are categorical and required explicit
encoding, while the remaining 9 are already numerical and required no
transformation: \texttt{CN\_CLUSTER}, \texttt{FRACTION\_GENOME\_ALTERED},
\texttt{METHYLATION\_CLUSTER}, \texttt{MIRNA\_CLUSTER},
\texttt{MUTATION\_COUNT}, \texttt{SIGCLUST\_UNSUPERVISED\_MRNA},
\texttt{TMB\_NONSYNONYMOUS}, \texttt{AGE}, and \texttt{OS\_MONTHS}.

Two fundamentally different types of categorical variables appear in this
dataset, and they call for different encoding strategies:
\begin{itemize}
    \item \textbf{Ordinal variables} carry a natural rank ordering among their
        categories: the categories are not merely distinct names but lie on a
        meaningful scale. For these variables, integer codes were assigned to
        \textit{preserve the natural clinical ordering}, so that the numerical
        distance between codes reflects the conceptual distance between categories.
        For example, \texttt{NODES} (lymph node involvement) follows the TNM
        classification N0 $<$ N1 $<$ N2 $<$ N3 in order of increasing severity, and
        was encoded accordingly as $\{0, 1, 2, 3\}$. Similarly, \texttt{TUMOR\_STAGE}
        was encoded as T1$\to$1, T2$\to$2, T3$\to$3, T4$\to$4 to preserve the
        size-based ordering.
    \item \textbf{Nominal variables} have categories that are qualitatively distinct but
        carry no inherent ordering. For binary nominal variables (such as
        \texttt{METASTASIS\_CODED} (Negative/Positive), \texttt{NODE\_CODED},
        \texttt{METASTASIS} (M0/M1), and \texttt{OS\_STATUS} (0:LIVING/1:DECEASED))
        standard 0/1 integer encoding was applied, since with only two categories
        the choice of which value maps to 0 or 1 is arbitrary and carries no
        structural implication. For multi-valued nominal variables with no meaningful
        ordering, a judgment call was required. \texttt{ER\_STATUS} and
        \texttt{PR\_STATUS}, for instance, each take four values
        (Negative, Positive, Performed but Not Available, Indeterminate), and
        \texttt{HER2\_STATUS} takes three (Negative, Equivocal, Positive). These were
        encoded with manual integer mappings. It should be noted that for
        \texttt{HER2\_STATUS}, there is a partial clinical ordering
        (Negative $<$ Equivocal $<$ Positive), and the encoding 0/1/2 was chosen to
        reflect this.
\end{itemize}

The complete encoding scheme for all 11 categorical features is listed in
Table~\ref{tab:encoding_scheme}. We can see that the target variable \texttt{PAM50\_SUBTYPE} was 
encoded at this stage with the following mapping:
Basal-like$\to$0, HER2-enriched$\to$1, Luminal~A$\to$2, Luminal~B$\to$3.
Normal-like samples received no mapping and were thus excluded from the
supervised modelling cohort, as noted in Section~\ref{subsec:missing_values}.
Upon completion of the encoding step, the dataset had the shape of \textbf{244
samples} described by \textbf{462 features} distributed in 20 clinical variables 
and 442
miRNA expression values - all represented as real numbers now.

\begin{table}[t]
\centering
\caption{Encoding scheme for the 11 categorical clinical features. Ordinal
features are highlighted; the ordering of the integer codes reflects the
natural clinical ranking.}
\label{tab:encoding_scheme}
\small
\begin{tabular}{p{3.8cm} p{1.5cm} p{7.0cm}}
\hline
\textbf{Feature} & \textbf{Type} & \textbf{Encoding} \\
\hline
\texttt{NODES}
    & Ordinal
    & N0$\to$0, N1$\to$1, N2$\to$2, N3$\to$3 \\
\texttt{TUMOR\_STAGE}
    & Ordinal
    & T1$\to$1, T2$\to$2, T3$\to$3, T4$\to$4 \\
\texttt{CONVERTED\_STAGE}
    & Ordinal
    & No\_Conversion$\to$0, Stage~I$\to$2, Stage~IIA$\to$1,
      Stage~IIB$\to$3, Stage~IIIA$\to$5, Stage~IIIB$\to$7,
      Stage~IIIC$\to$4, Stage~IV$\to$6 \\
\texttt{HER2\_STATUS}
    & Ordinal*
    & Negative$\to$0, Equivocal$\to$1, Positive$\to$2 \\
\texttt{ER\_STATUS}
    & Nominal
    & Negative$\to$0, Performed but Not Available$\to$1,
      Positive$\to$2, Indeterminate$\to$3 \\
\texttt{PR\_STATUS}
    & Nominal
    & Negative$\to$0, Positive$\to$1,
      Performed but Not Available$\to$2, Indeterminate$\to$3 \\
\texttt{TUMOR\_T1\_CODED}
    & Binary
    & T\_Other$\to$0, T1$\to$1 \\
\texttt{NODE\_CODED}
    & Binary
    & Negative$\to$0, Positive$\to$1 \\
\texttt{METASTASIS\_CODED}
    & Binary
    & Negative$\to$0, Positive$\to$1 \\
\texttt{METASTASIS}
    & Binary
    & M0$\to$0, M1$\to$1 \\
\texttt{OS\_STATUS}
    & Binary
    & 0:LIVING$\to$0, 1:DECEASED$\to$1 \\
\hline
\multicolumn{3}{l}{\footnotesize * Partial ordering exists clinically; encoding reflects severity progression.} \\
\end{tabular}
\end{table}


% ---------------------------------------------------------------
\subsection{Train/Test Split and Prevention of Data Leakage}
\label{subsec:split}
% ---------------------------------------------------------------

The final processing step before model training was the partition of the
dataset into a training set and a held-out test set, followed by feature
standardisation. The order in which these operations are performed is not a
minor implementation detail: it is a methodologically critical design decision
that determines whether reported performance metrics are trustworthy estimates
of generalisation ability or inflated artefacts of inadvertent information
leakage.

\subsubsection*{Stratified Splitting \& Data Leakage Prevention}

To ensure a robust evaluation, the dataset was divided into an \textbf{80\% training set} 
(195 samples) and a \textbf{20\% test set} (49 samples) using stratified random 
sampling. Stratification was essential to maintain the proportion of each PAM50 
subtype across both partitions, thereby preventing the accidental over-representation 
or under-representation of minority classes: a critical safeguard given the class 
imbalance documented in Section~\ref{subsec:working_cohort}. The resulting class 
distributions, which demonstrate the consistency of this split, are shown in 
Figure~\ref{fig:subtype_histogram_sub}.

\begin{figure}[t]
    \centering
    % --- LADO ESQUERDO: TABELA ---
    \begin{minipage}[c]{0.52\textwidth}
        \centering
        \resizebox{\textwidth}{!}{%
        \begin{tabular}{lr}
        \toprule
        \textbf{Property} & \textbf{Value} \\
        \midrule
        \multicolumn{2}{l}{\textit{Dimensionality}} \\
        \quad Total samples                                  & 244 \\
        \quad miRNA features                                 & 442 \\
        \quad Clinical features                              & 20 \\
        \quad Total features                                 & 462 \\
        \quad Feature-to-sample ratio ($p/n$)                & 1.89 \\
        \midrule
        \multicolumn{2}{l}{\textit{Data quality}} \\
        \quad Missing values                                 & 0 \\
        \quad miRNAs with variance $> 1.0$                   & 250 \;(56.6\%) \\
        \midrule
        \multicolumn{2}{l}{\textit{miRNA expression (log$_2$ scale)}} \\
        \quad Range                                          & $[-2.37,\; 18.66]$ \\
        \quad Mean $\pm$ std                                 & $4.87 \pm 3.99$ \\
        \quad Median                                         & 4.07 \\
        \quad Values $< 0$ (low-abundance artefact)          & 7.1\% \\
        \midrule
        \multicolumn{2}{l}{\textit{Clinical continuous features}} \\
        \quad Age (years)                                    & $59.7 \pm 13.4$ \;\;[26, 89] \\
        \quad Mutation count                                 & $45.7 \pm 36.3$ \;\;[10, 223] \\
        \quad Overall survival (months)                      & $34.3 \pm 41.4$ \;\;[0, 234] \\
        \quad Fraction genome altered                        & $0.34 \pm 0.21$ \;\;[0.00, 0.95] \\
        \quad TMB non-synonymous                             & $1.53 \pm 1.21$ \;\;[0.30, 7.40] \\
        \bottomrule
        \end{tabular}
        }
        \subcaption{Summary statistics of the final dataset.}
        \label{tab:dataset_summary}
    \end{minipage}
    \hfill
    % --- LADO DIREITO: GRÁFICO ---
    \begin{minipage}[c]{0.45\textwidth}
        \centering
        \resizebox{\textwidth}{!}{%
        \begin{tikzpicture}
            \begin{axis}[
                ybar,
                enlargelimits=0.15,
                legend style={
                    at={(0.5,-0.35)}, 
                    anchor=north,
                    legend columns=-1,
                    font=\small
                },
                ylabel={Number of Samples},
                symbolic x coords={Basal, Her2, Luminal A, Luminal B},
                xtick=data,
                x tick label style={rotate=45, anchor=east, font=\small},
                nodes near coords,
                nodes near coords style={font=\tiny},
                nodes near coords align={vertical},
                ]
                
                \addplot[fill=gray!30] coordinates {(Basal,41) (Her2,29) (Luminal A,103) (Luminal B,71)};
                \addplot[fill=blue!30] coordinates {(Basal,33) (Her2,23) (Luminal A,82) (Luminal B,57)};
                \addplot[fill=red!30] coordinates {(Basal,8) (Her2,6) (Luminal A,21) (Luminal B,14)};
                
                \legend{Total, Train, Test}
            \end{axis}
        \end{tikzpicture}
        }
        \subcaption{Subtype distribution histogram.}
        \label{fig:subtype_histogram_sub}
    \end{minipage}

    \vspace{10pt}
    \caption{Overview of the final modelling dataset: (a) detailed summary of clinical and molecular features; (b) distribution of PAM50 subtypes across the full dataset and the stratified train/test partitions.}
    \label{fig:dataset_overview_full}
\end{figure}

This partitioning serves as the primary defense against \textbf{data leakage}, 
which occurs when information from outside the training set is used during the 
learning process. Leakage is a frequent source of overly optimistic performance 
estimates, as the model inadvertently learns patterns from data that would be 
unavailable at inference time. In this pipeline, the prevention of leakage was 
strictly enforced, particularly regarding \textbf{scaling leakage}. This common 
pitfall manifests if a \textit{StandardScaler} is fitted on the full dataset before the 
split; in such cases, the scaling parameters (the mean $\mu$ and standard 
deviation $\sigma$ per feature) would be derived from both training and test 
samples. By ensuring the split occurs before any statistic-based transformation, 
the test set remains truly unseen, preserving the independence assumption 
required for a meaningful evaluation.

To prevent this, the following protocol was strictly enforced throughout all
experiments:

\begin{enumerate}
    \item The 80/20 stratified split was performed \textbf{first}, before any
    scaling parameter was computed.

    \item A \textit{StandardScaler} was then fitted \textbf{exclusively on the
    training fold}, computing the per-feature mean $\mu_{\text{train}}$ and
    standard deviation $\sigma_{\text{train}}$ from training samples only.

    \item The transformation derived from the training fold was then applied
    \textbf{without re-fitting} to both the training and test folds:
    \begin{equation}
        z = \frac{x - \mu_{\text{train}}}{\sigma_{\text{train}}}
        \label{eq:zscore}
    \end{equation}
    where $z$ is the standardised feature value and $x$ is the original value.
    The test fold is thus transformed using training statistics, simulating the
    situation at deployment time where the model encounters data whose
    distribution it has not observed.
\end{enumerate}

Standardisation was applied to two groups of features: the five continuous
clinical variables with heterogeneous scales (\texttt{MUTATION\_COUNT},
\texttt{SIGCLUST\_UNSUPERVISED\_MRNA}, \texttt{TMB\_NONSYNONYMOUS},
\texttt{AGE}, \texttt{OS\_MONTHS}), and all 442 miRNA expression columns.
The remaining clinical features, those already encoded as small integers
(representing ordinal or binary categories) were left unscaled as their
values are already bounded and comparable in magnitude.

Raw miRNA expression values span the range $[-2.37,\, 18.66]$ on the
log$_2$ scale, with a mean of $4.97$ and a standard deviation of $4.03$.
Clinical continuous features operate on entirely different numerical scales:
\texttt{MUTATION\_COUNT} ranges from 10 to 223, \texttt{OS\_MONTHS} from 0 to
234, and \texttt{FRACTION\_GENOME\_ALTERED} from 0 to 0.95. Without
standardisation, algorithms sensitive to feature magnitude, such as
distance-based methods and gradient-based optimisers, would disproportionately
weight high-magnitude features irrespective of their actual predictive value.

% ================================================================

% ============================================================
% CHAPTER: SUPERVISED APPROACH
% ============================================================
\section{Discriminative Subtyping}
\label{sec:supervised}

As described in Chapter~\ref{chap:data-preprocessing}, the dataset used throughout this work
consists of \textbf{244 patient samples} sourced from TCGA, each characterised by two distinct
feature modalities: \textbf{445 miRNA expression features} and \textbf{17 clinical features}
(including hormonal receptor status, copy-number cluster assignments, and
methylation cluster labels), together with a PAM50-derived subtype label
(\textit{Basal-like}, \textit{HER2-enriched}, \textit{Luminal~A}, or \textit{Luminal~B}).
After the preprocessing pipeline detailed in the previous chapter, the data was 
organised into training and test partitions that are used 
consistently across all experiments reported here.

The present chapter focuses on the \textbf{supervised learning component} of this thesis.
The central objective is twofold: first, to determine whether machine learning classifiers
can reliably distinguish breast cancer subtypes from miRNA expression data alone, and more
importantly, whether the addition of clinical covariates improves that discrimination;
second, and most crucially, to \textbf{identify the minimal set of miRNA (and clinical)
features that best characterise each subtype}. This second objective aligns directly with
the broader biological question motivating this work: \textbf{which miRNAs carry subtype-discriminative
information, and can machine learning provide a computationally grounded way to answer it?}

% ============================================================
\subsection{Exploratory Model Comparison}
\label{sec:early-models}
% ============================================================

A initial exploratory phase was conducted to assess 
which classifiers were most suitable for the multiclass PAM50 subtype prediction task.
The four classifiers evaluated were \textbf{Decision Tree}, \textbf{Logistic Regression},
\textbf{XGBoost}, and \textbf{DIABLO}. The selection of these four models was deliberate,
as the first three are established baselines that span the spectrum of model complexity,
from a single-split decision surface to a regularised linear model to a full gradient-boosted
ensemble, and are routinely used for benchmarking on tabular genomic data and classification
problems. DIABLO was included on the recommendation of collaborators from Nova Medical School,
as it is a well-established method in the multi-omics classification literature~\cite{singh2019diablo},
explicitly designed to integrate heterogeneous data blocks, in this case, miRNA expression
and clinical features, through a sparse latent-variable structure.
 
A criterion that shaped the selection equally alongside predictive performance was
\textbf{interpretability}. The ultimate aim of this supervised approach is not classification
per se, but the identification of the features - specially the miRNAs proven to be 
great biomarkers - that best
characterise each subtype. A model that classifies accurately but obscures its decision
process provides limited biological insight and brings lower value to the table. This requirement 
favoured models with accessible
and meaningful feature importance mechanisms, a point that would later prove decisive in
choosing XGBoost as the final architecture.
 
All four models were evaluated on \textbf{both feature configurations} (miRNA only and
miRNA + clinical data) to assess whether the inclusion
of clinical covariates represented a worthwhile investment for improving discrimination.
The Logistic Regression was configured with an L2 penalty (\texttt{C}~=~1.0,
\texttt{solver}~=~\texttt{lbfgs}, \texttt{multi\_class}~=~\texttt{multinomial}) and the
initial XGBoost used 100 estimators with a learning rate of 0.1 and a maximum tree depth
of 3, this would later be our starting point before grid search
refinement. Table~\ref{tab:early-models}, extracted from our published paper  reports the results on the miRNA + clinical configuration
which represented the more informative of the two conditions for this comparison.

\todo{Correr o código para gerar os resultados com k-fold cross-validation para mostrar que cada modelo é robusto e não overfit}

\todo{Corrigir o texto abaixo: a conclusão vai ser a mesma, mas o texto vai ser diferente}
The results present a clear ranking. XGBoost achieved the highest overall performance,
with a macro F1-score of 82.17\% and an accuracy of 83.67\%, followed by Logistic Regression
(macro F1~=~75.57\%) and DIABLO (macro F1~=~73.82\%). The Decision Tree lagged behind
considerably at a macro F1-score of 62.45\%, reflecting its tendency to overfit in the
high-dimensional setting. Across all models, Basal-like was the most consistently
well-classified subtype, while HER2-enriched and Luminal~B remained the most difficult -
a pattern that would persist through subsequent refinements and is discussed at length
in Section~\ref{sec:discussion}.
 
DIABLO's performance is worth brief comment. Despite being specifically designed for
multi-omics integration, it did not surpass Logistic Regression and fell clearly
behind XGBoost. This is partly attributable to the relatively small sample size
($n = 244$), which limits the ability of latent-variable models to reliably estimate
cross-block covariance structure. More importantly for our purposes, DIABLO's sparse
latent components offer a different form of interpretability than per-feature importance
scores, making it less directly applicable to the goal of identifying individual miRNA
candidates. XGBoost, by contrast, provides gain-based feature importances at the level
of individual features, which aligns precisely with the analytical objective of this work.
 
These results established XGBoost as the clear candidate for further development,
both on grounds of performance and interpretability, motivating the hyperparameter
refinement described in the following section.

% ============================================================
\subsection{Final Model Architecture: XGBoost with Grid Search}
\label{sec:xgboost-final}
% ============================================================

Given XGBoost's competitive performance in the exploratory phase, we will proceed with a systematic
hyperparameter search with grid search to identify its optimal configuration for
the breast cancer subtype classification task. Grid search is a technique that systematically 
searches for the best hyperparameters by exhaustively evaluating a grid of values for each hyperparameter.
The parameters that we aim to tune in the grid search are:

\begin{itemize}
    \item \textbf{Number of estimators} (\texttt{n\_estimators}): number of boosting rounds
    (trees) in the ensemble. A larger value allows a more expressive model at the cost of
    increased training time and potential overfitting.

    \item \textbf{Learning rate} (\texttt{learning\_rate}): the shrinkage applied to each
    tree's contribution. Smaller values require more trees but generally improve generalisation.

    \item \textbf{Maximum tree depth} (\texttt{max\_depth}): controls model complexity.
    Deeper trees capture higher-order interactions but overfit more easily.

    \item \textbf{Row subsampling} (\texttt{subsample}): the fraction of training samples
    used per boosting round, introducing stochasticity to reduce variance.

    \item \textbf{Column subsampling} (\texttt{colsample\_bytree}): the fraction of features
    randomly selected at each boosting round, analogous to the feature bagging in Random Forests.
\end{itemize}

The search was performed using stratified cross-validation with macro F1-score as the optimisation criterion.
The resulting optimal configuration was:

\begin{center}
\begin{tabular}{ll}
\hline
\textbf{Hyperparameter} & \textbf{Value} \\
\hline
\texttt{n\_estimators}    & 50   \\
\texttt{learning\_rate}   & 0.3  \\
\texttt{max\_depth}       & 10   \\
\texttt{subsample}        & 0.85 \\
\texttt{colsample\_bytree}& 1.0  \\
\texttt{eval\_metric}     & \texttt{mlogloss} \\
\texttt{random\_state}    & 42   \\
\hline
\end{tabular}
\end{center}

The results achieved using this configuration will be analysed in detail in 
Section~\ref{sec:model-results}, with a complementary exploration available in our 
published paper~\ref{ann:EPIA_paper} where we evaluate the statistical significance 
of the improvement in performance when adding clinical data. 

% ============================================================
\subsection{Two-Level Validation Hypothesis}
\label{sec:validation-hypothesis}
% ============================================================

Before presenting the model results, it is important to take a step back and 
reflect that our primary question is not merely
\textit{which features help the classifier}, but rather \textit{which features are genuinely
informative for subtype discrimination}. Answering this question robustly requires more than
a single source of evidence.

To this end, we propose the following hypothesis:

\begin{quote}
\textit{A miRNA (or clinical feature) is considered a \textbf{robustly validated discriminative
marker} for a given breast cancer subtype if and only if it is independently selected by two
complementary feature identification methods operating under fundamentally different assumptions:
(i) the XGBoost internal feature importance, which reflects discriminative contribution within
a trained multivariate model, and (ii) the ANOVA F-test, which reflects marginal statistical
significance under the assumption of between-group mean differences.}
\end{quote}

The rationale for this two-level approach stems directly from the complementary nature
of the two methods:

\begin{itemize}
    \item \textbf{ANOVA F-test (filter method)}: a model-agnostic statistical test that
    evaluates whether the means of a given feature differ significantly across subtype groups.
    A high F-score indicates that a feature is statistically distinguishable at the population
    level. Crucially, ANOVA operates on each feature in isolation, ignoring feature interactions.
    Features selected by ANOVA can therefore be interpreted as carrying \textit{individually
    significant statistical signal} for subtype discrimination.

    \item \textbf{XGBoost gain importance (embedded method)}: the total gain attributed to a
    feature across all splits in the ensemble. Features with high gain are those that, in a
    multivariate discriminative context, contribute most to reducing classification error. This
    measure captures feature utility in the presence of all other features, including interactions and redundancies.
\end{itemize}

A feature appearing in \textit{both} ranked lists is therefore supported by two independent
confirmation levels: it is \textit{statistically significant in isolation} and
\textit{discriminatively relevant in a multivariate learned model}. This double 
confirmation forms a particularly powerful filter for biomarker discovery: features 
verified by both methods are not only statistically significant in isolation but also 
deliver real, independent discrimination when evaluated inside the learning algorithm. 
Such convergence across distinct methodologies offers compelling support for prioritising 
these features as leading biomarker candidates. As shown in 
Section~\ref{sec:model-results} and highlighted in Table~\ref{tab:intersections}, 
doubly-validated features are consistently associated with stronger classification 
performance and robustness across subtypes, providing persuasive evidence that goes 
beyond what any single method could deliver alone, giving us confidence in the 
features identified by our approach and consider them strong candidates for 
further empirical validation. 

% ============================================================
\subsection{Model Results}
\label{sec:model-results}
% ============================================================

The optimised XGBoost model was evaluated under a binary One-vs-Rest (OvR) formulation:
for each breast cancer subtype, a separate binary classifier was trained to distinguish
patients of that subtype from all others. This One-vs-Rest strategy was adopted to enable
per-subtype feature extraction (see Section~\ref{sec:booster-features}) and to produce
interpretable per-subtype performance metrics - after all, this models main purpose is to 
classify if a patient has a specific subtype or not.

Both feature configurations were evaluated: \textbf{miRNA only} (445 features) and
\textbf{miRNA + clinical} (445 miRNA + 17 clinical features = 462 features).

% ==========================================
% 1. BASAL-LIKE SUBTYPE
% ==========================================
\subsubsection{Basal-Like}

The Basal-like subtype yielded the highest classification performance across both configurations,
with both miRNA-only and miRNA+clinical models achieving precision, recall, and F1-score of 1.00
on the test set. This result reflects the \textbf{strong and distinct miRNA expression signature}
characteristic of Basal-like tumours, which are triple-negative, highly proliferative, and
genomically distinct from the other subtypes.

\begin{table}[h]
    \centering
    \caption{Classification Performance for Basal-like Subtype}
    \begin{subtable}{0.48\textwidth}
        \centering
        \caption{Homogeneous Data}
        \begin{tabular}{lcccc}
            \toprule
            & prec. & rec. & f1 & supp. \\
            \midrule
            Not Basal & 1.00 & 1.00 & 1.00 & 41 \\
            Basal     & 1.00 & 1.00 & 1.00 & 8 \\
            \midrule
            accuracy  &      &      & 1.00 & 49 \\
            \bottomrule
        \end{tabular}
    \end{subtable}
    \hfill
    \begin{subtable}{0.48\textwidth}
        \centering
        \caption{Heterogeneous Data}
        \begin{tabular}{lcccc}
            \toprule
            & prec. & rec. & f1 & supp. \\
            \midrule
            Not Basal & 1.00 & 1.00 & 1.00 & 41 \\
            Basal     & 1.00 & 1.00 & 1.00 & 8 \\
            \midrule
            accuracy  &      &      & 1.00 & 49 \\
            \bottomrule
        \end{tabular}
    \end{subtable}
\end{table}

\begin{figure}[h]
    \centering
    % --- First Matrix (Left Picture) ---
    % \includegraphics is set to use almost half the text width (0.48)
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        % REPLACE THE FILENAME BELOW WITH YOUR ACTUAL IMAGE FILE
        \includegraphics[width=\textwidth]{cm_basal_small_without.png}
        % This is the specific caption for the first matrix
        \caption{}
    \end{subfigure}
    \hfill % Adds flexible space between the two pictures to fill the page margins
    % --- Second Matrix (Right Picture) ---
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        % REPLACE THE FILENAME BELOW WITH YOUR ACTUAL IMAGE FILE
        \includegraphics[width=\textwidth]{cm_basal_small_with.png}
        % This is the specific caption for the second matrix
        \caption{}
    \end{subfigure}

    % --- Main Caption for the Entire Figure ---
    \vfill
    \caption{Comparison of Classification Performance for Basal-like Breast Cancer Subtype classification where (a) has only miRNA data and (b) has both miRNA and clinical data.}
    \label{fig:comparison_matrices}
\end{figure}


% ==========================================
% 2. HER2-ENRICHED SUBTYPE
% ==========================================
\subsubsection{HER2-Enriched}

HER2-enriched classification proved to be more challenging. The miRNA-only model achieved an F1-score
of 0.50 for the HER2-enriched class (precision 1.00, recall 0.33), indicating that while the
model is highly precise - it does not produce false positives - it misses a substantial
fraction of true HER2 cases (low recall). The macro-averaged F1-score across both classes
was 0.73 (miRNA only) and remained similar with clinical data.

This is a known difficulty in the literature: HER2-enriched tumours exhibit considerable
transcriptomic overlap with Luminal~B, and their molecular distinctions are more reliably
captured through protein expression or copy-number amplification of the \textit{ERBB2} locus
rather than through miRNA profiles alone. The addition of clinical features - particularly
\texttt{HER2\_STATUS} - provides meaningful supplementary signal for this subtype, as
confirmed in the ANOVA analysis (Section~\ref{sec:anova-bridge}).


\begin{table}[htbp]
    \centering
    \caption{Classification Performance for HER2-enriched Subtype}
    \begin{subtable}{0.48\textwidth}
        \centering
        \caption{Homogeneous Data}
        \begin{tabular}{lcccc}
            \toprule
            & prec. & rec. & f1 & supp. \\
            \midrule
            Not HER2 & 0.91 & 1.00 & 0.96 & 43 \\
            HER2     & 1.00 & 0.33 & 0.50 & 6 \\
            \midrule
            accuracy &      &      & 0.92 & 49 \\
            \bottomrule
        \end{tabular}
    \end{subtable}
    \hfill
    \begin{subtable}{0.48\textwidth}
        \centering
        \caption{Heterogeneous Data}
        \begin{tabular}{lcccc}
            \toprule
            & prec. & rec. & f1 & supp. \\
            \midrule
            Not HER2 & 0.91 & 1.00 & 0.96 & 43 \\
            HER2     & 1.00 & 0.33 & 0.50 & 6 \\
            \midrule
            accuracy &      &      & 0.92 & 49 \\
            \bottomrule
        \end{tabular}
    \end{subtable}
\end{table}

\begin{figure}[h]
    \centering
    % --- First Matrix (Left Picture) ---
    % \includegraphics is set to use almost half the text width (0.48)
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        % REPLACE THE FILENAME BELOW WITH YOUR ACTUAL IMAGE FILE
        \includegraphics[width=\textwidth]{cm_her2_small_without.png}
        % This is the specific caption for the first matrix
        \caption{}
    \end{subfigure}
    \hfill % Adds flexible space between the two pictures to fill the page margins
    % --- Second Matrix (Right Picture) ---
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        % REPLACE THE FILENAME BELOW WITH YOUR ACTUAL IMAGE FILE
        \includegraphics[width=\textwidth]{cm_her2_small_with.png}
        % This is the specific caption for the second matrix
        \caption{}
    \end{subfigure}

    % --- Main Caption for the Entire Figure ---
    \vfill
    \caption{Comparison of Classification Performance for Her2-enriched Breast Cancer Subtype classification where (a) has only miRNA data and (b) has both miRNA and clinical data.}
    \label{fig:comparison_matrices}
\end{figure}

% ==========================================
% 3. LUMINAL A SUBTYPE
% ==========================================
\subsubsection{Luminal A}

Luminal A classification yielded a weighted-average F1-score of 0.84 (miRNA only) 
and 0.82 (miRNA + clinical), with per-class F1-scores of 0.81 and 0.80, respectively. 
The slight decrease when adding clinical data warrants attention: it may reflect 
that clinical features introduce noise or redundancy for a subtype whose miRNA 
signature is already relatively distinct.

\begin{table}[htbp]
    \centering
    \caption{Classification Performance for Luminal A Subtype}
    \begin{subtable}{0.48\textwidth}
        \centering
        \caption{Homogeneous Data}
        \begin{tabular}{lcccc}
            \toprule
            & prec. & rec. & f1 & supp. \\
            \midrule
            Not LumA & 0.86 & 0.86 & 0.86 & 28 \\
            Luminal A & 0.81 & 0.81 & 0.81 & 21 \\
            \midrule
            accuracy &      &      & 0.84 & 49 \\
            \bottomrule
        \end{tabular}
    \end{subtable}
    \hfill
    \begin{subtable}{0.48\textwidth}
        \centering
        \caption{Heterogeneous Data}
        \begin{tabular}{lcccc}
            \toprule
            & prec. & rec. & f1 & supp. \\
            \midrule
            Not LumA & 0.88 & 0.79 & 0.83 & 28 \\
            Luminal A & 0.75 & 0.86 & 0.80 & 21 \\
            \midrule
            accuracy &      &      & 0.82 & 49 \\
            \bottomrule
        \end{tabular}
    \end{subtable}
\end{table}

\begin{figure}[h]
    \centering
    % --- First Matrix (Left Picture) ---
    % \includegraphics is set to use almost half the text width (0.48)
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        % REPLACE THE FILENAME BELOW WITH YOUR ACTUAL IMAGE FILE
        \includegraphics[width=\textwidth]{cm_lumA_small_without.png}
        % This is the specific caption for the first matrix
        \caption{}
    \end{subfigure}
    \hfill % Adds flexible space between the two pictures to fill the page margins
    % --- Second Matrix (Right Picture) ---
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        % REPLACE THE FILENAME BELOW WITH YOUR ACTUAL IMAGE FILE
        \includegraphics[width=\textwidth]{cm_lumA_small_with.png}
        % This is the specific caption for the second matrix
        \caption{}
    \end{subfigure}

    % --- Main Caption for the Entire Figure ---
    \vfill
    \caption{Comparison of Classification Performance for Luminal A Breast Cancer Subtype classification where (a) has only miRNA data and (b) has both miRNA and clinical data.}
    \label{fig:comparison_matrices}
\end{figure}

% ==========================================
% 4. LUMINAL B SUBTYPE
% ==========================================
\subsubsection{Luminal B}

Luminal~B was the most difficult subtype to classify, with an F1-score of 0.50 for the
Luminal~B class under the miRNA-only configuration (recall 0.36). With clinical data, the
macro-averaged F1-score improved from 0.69 to 0.77, a notable gain suggesting that clinical
covariates (particularly \texttt{ER\_STATUS}) carry discriminative information not present
in miRNA expression alone. This aligns with the known molecular heterogeneity of Luminal~B,
which overlaps substantially with Luminal~A and lacks a clean miRNA-only discriminative boundary.

\begin{table}[htbp]
    \centering
    \caption{Classification Performance for Luminal B Subtype}
    \begin{subtable}{0.48\textwidth}
        \centering
        \caption{Homogeneous Data}
        \begin{tabular}{lcccc}
            \toprule
            & prec. & rec. & f1 & supp. \\
            \midrule
            Not LumB & 0.79 & 0.97 & 0.87 & 35 \\
            Luminal B & 0.83 & 0.36 & 0.50 & 14 \\
            \midrule
            accuracy &      &      & 0.80 & 49 \\
            \bottomrule
        \end{tabular}
    \end{subtable}
    \hfill
    \begin{subtable}{0.48\textwidth}
        \centering
        \caption{Heterogeneous Data}
        \begin{tabular}{lcccc}
            \toprule
            & prec. & rec. & f1 & supp. \\
            \midrule
            Not LumB & 0.83 & 0.97 & 0.89 & 35 \\
            Luminal B & 0.88 & 0.50 & 0.64 & 14 \\
            \midrule
            accuracy &      &      & 0.84 & 49 \\
            \bottomrule
        \end{tabular}
    \end{subtable}
\end{table}

\begin{figure}[h]
    \centering
    % --- First Matrix (Left Picture) ---
    % \includegraphics is set to use almost half the text width (0.48)
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        % REPLACE THE FILENAME BELOW WITH YOUR ACTUAL IMAGE FILE
        \includegraphics[width=\textwidth]{cm_lumB_small_without.png}
        % This is the specific caption for the first matrix
        \caption{}
    \end{subfigure}
    \hfill % Adds flexible space between the two pictures to fill the page margins
    % --- Second Matrix (Right Picture) ---
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        % REPLACE THE FILENAME BELOW WITH YOUR ACTUAL IMAGE FILE
        \includegraphics[width=\textwidth]{cm_lumB_small_with.png}
        % This is the specific caption for the second matrix
        \caption{}
    \end{subfigure}

    % --- Main Caption for the Entire Figure ---
    \vfill
    \caption{Comparison of Classification Performance for Luminal B Breast Cancer Subtype classification where (a) has only miRNA data and (b) has both miRNA and clinical data.}
    \label{fig:comparison_matrices}
\end{figure}

% ============================================================
\subsection{Feature Extraction via XGBoost Booster}
\label{sec:booster-features}
% ============================================================

Having validated the classification performance of the optimised model, attention turns to the
primary scientific objective: identifying the miRNAs most relevant to each subtype. To this end,
the XGBoost internal booster was used to compute per-subtype \textbf{feature importances based
on gain} - the average improvement in the loss function attributable to a feature across all
splits in which it is used.

The binary OvR formulation is central here: by training a separate binary classifier for each
subtype, the feature importances extracted are specific to the problem of distinguishing
\textit{that subtype from all others}, rather than reflecting an aggregate multiclass importance.
This provides subtype-specific miRNA rankings.

Formally, for a given subtype $s \in \{\text{Basal-like}, \text{HER2-enriched},
\text{Luminal~A}, \text{Luminal~B}\}$, the binary label is defined as:

\begin{equation}
y_i^{(s)} = \begin{cases} 1 & \text{if patient } i \text{ belongs to subtype } s \\ 0 & \text{otherwise} \end{cases}
\end{equation}

The XGBoost booster was retrieved via \texttt{model.get\_booster()} and gain importances
were computed via \texttt{booster.get\_score(importance\_type=\text{`gain'})}, returning a
dictionary mapping each feature to its cumulative gain across all trees. The top 20 features
by gain were extracted for each subtype and each configuration.

\noindent \textbf{Gain} measures the total reduction in the model's objective function 
(loss) contributed by a feature across all trees. Unlike other metrics, it focuses on the 
\textit{quality} of splits rather than their frequency:

\begin{description}
    \item[Gain:] The accumulated improvement in accuracy or error reduction provided by a feature.
    \item[Weight:] The raw count of how many times a feature is used as a split.
    \item[Cover:] The average number of samples or observations affected by a feature's splits.
\end{description}

While \textit{weight} tells us how often a feature is used, \textbf{gain} tells us how
 much that feature actually matters to the final prediction - and this is exactly why we
  used the \textbf{gain} for our metric of feature importance selection.

Table~\ref{tab:feature_importance_top20} presents the top 20 miRNA features ranked 
by gain for each breast cancer subtype, under both the miRNA-only and miRNA + clinical 
configurations. For each subtype, the table reflects the features that the 
XGBoost model identified as most informative when learning to distinguish 
that subtype from all others, that is, the variables whose inclusion in 
the decision trees led to the greatest cumulative reduction in classification 
error. The gain values also convey the relative importance of each feature: 
a feature with substantially higher gain than its neighbours can be interpreted 
as carrying a disproportionate share of the discriminative signal for that 
subtype, whereas a more gradual decay in gain values suggests that the 
classification relies on a broader, more distributed set of features. 
Reading across subtypes, the table thus provides a direct window into 
the molecular signatures that the model has learned to associate with each class.

\begin{table}[t]
\centering
\caption{Top 20 XGBoost Feature Importance: miRNA Only vs. miRNA + Clinical Integration (\% Gain)}
\label{tab:feature_importance_percent}
\scriptsize
\begin{tabular}{r|ll|ll}
\hline
\textbf{Rank} & \multicolumn{2}{c|}{\textbf{Basal-like}} & \multicolumn{2}{c}{\textbf{HER2-enriched}} \\
& \textit{miRNA Only} & \textit{miRNA + Clinical} & \textit{miRNA Only} & \textit{miRNA + Clinical} \\ \hline
1  & MIR-577/577 (59.2\%)   & MIR-577/577 (56.9\%)   & MIR-30/30A (15.2\%)    & MIR-30/30A (10.7\%)    \\
2  & MIR-135B/135B* (9.5\%) & MIR-135B/135B* (10.5\%) & MIR-34C/5P (7.5\%)      & MIR-3150B/3P (9.8\%)   \\
3  & MIR-934/934 (8.5\%)    & MIR-934/934 (7.7\%)    & MIR-501/3P (6.9\%)      & MIR-34C/5P (8.4\%)      \\
4  & MIR-190B/190B (4.3\%)   & \textbf{METHYLATION\_CL.} (4.1\%) & MIR-769/5P (6.2\%)      & MIR-769/5P (6.5\%)      \\
5  & MIR-455/5P (2.8\%)      & MIR-190B/190B (3.8\%)   & MIR-192/192* (6.1\%)    & MIR-192/192* (5.4\%)    \\
6  & MIR-584/584 (2.0\%)     & MIR-455/5P (3.4\%)      & MIR-3150B/3P (4.8\%)    & MIR-501/3P (4.7\%)      \\
7  & MIR-505/505 (1.9\%)     & MIR-505/505 (1.9\%)     & MIR-187/187 (3.7\%)     & MIR-187/187 (4.3\%)     \\
8  & MIR-19B-1/19B (1.9\%)   & MIR-2115/2115* (1.8\%)  & MIR-30C-2/2* (3.5\%)    & MIR-210/210 (3.1\%)     \\
9  & MIR-2115/2115* (1.6\%)  & \textbf{SIGCLUST\_MRNA} (1.6\%) & MIR-142/5P (2.8\%)      & MIR-142/3P (3.0\%)      \\
10 & MIR-9-1/9* (1.3\%)      & \textbf{CN\_CLUSTER} (1.0\%) & MIR-375/375 (2.7\%)     & \textbf{HER2\_STATUS} (2.9\%) \\
11 & MIR-29/29A (1.0\%)      & MIR-516-1/5P (0.8\%)    & MIR-210/210 (2.6\%)     & MIR-497/497* (2.7\%)    \\
12 & MIR-1296/1296 (0.9\%)   & MIR-9-1/9* (0.7\%)      & MIR-142/3P (2.6\%)      & MIR-184/184 (2.6\%)     \\
13 & MIR-194-2/194* (0.7\%)  & MIR-455/3P (0.7\%)      & MIR-30C/3P (2.6\%)      & MIR-193/5P (2.5\%)      \\
14 & MIR-501/3P (0.7\%)      & MIR-519A-1/519A* (0.6\%) & MIR-2115/2115* (2.4\%)  & MIR-30C/3P (2.1\%)      \\
15 & MIR-455/3P (0.6\%)      & MIR-24-2/2* (0.6\%)     & MIR-323B/3P (2.0\%)     & MIR-511/3P (2.1\%)      \\
16 & MIR-675/675* (0.5\%)    & MIR-660/660 (0.6\%)     & MIR-301/301A (1.9\%)    & MIR-577/577 (2.0\%)     \\
17 & MIR-744/744* (0.5\%)    & MIR-30C/3P (0.6\%)      & MIR-135-1/135A (1.9\%)  & MIR-651/651 (2.0\%)     \\
18 & MIR-9-1/9 (0.5\%)       & MIR-20A/20A* (0.6\%)    & MIR-134/134 (1.8\%)     & MIR-181D/181D (1.9\%)   \\
19 & MIR-18A/18A* (0.5\%)    & MIR-589/5P (0.5\%)      & MIR-217/217 (1.7\%)     & MIR-134/134 (1.9\%)     \\
20 & MIR-519A-1/519A* (0.4\%) & MIR-18A/18A* (0.5\%)    & MIR-184/184 (1.7\%)     & MIR-2115/2115* (1.9\%)  \\ \hline
\textbf{Rank} & \multicolumn{2}{c|}{\textbf{Luminal A}} & \multicolumn{2}{c}{\textbf{Luminal B}} \\
& \textit{miRNA Only} & \textit{miRNA + Clinical} & \textit{miRNA Only} & \textit{miRNA + Clinical} \\ \hline
1  & MIR-30/30A (5.1\%)     & MIR-30/30A (5.4\%)     & MIR-3677/3P (5.1\%)    & MIR-190B/190B (5.5\%)  \\
2  & MIR-501/3P (4.8\%)     & MIR-19A/19A (5.0\%)    & MIR-190B/190B (4.9\%)  & MIR-3677/3P (5.0\%)    \\
3  & MIR-19A/19A (4.8\%)    & MIR-501/3P (4.9\%)     & MIR-126/126 (4.1\%)    & MIR-3615/3615 (3.8\%)   \\
4  & MIR-1301/1301 (4.6\%)   & MIR-28/3P (4.2\%)       & MIR-3615/3615 (3.9\%)   & MIR-206/206 (3.7\%)     \\
5  & MIR-15B/15B* (4.0\%)    & MIR-1301/1301 (4.1\%)   & MIR-1251/1251 (3.2\%)   & MIR-25/25 (3.5\%)       \\
6  & MIR-28/3P (4.0\%)       & MIR-125A/3P (4.0\%)     & MIR-19B-1/1* (3.1\%)    & \textbf{SIGCLUST\_MRNA} (3.4\%) \\
7  & MIR-125A/3P (3.8\%)     & MIR-15B/15B* (3.6\%)    & MIR-99/99A (2.8\%)      & MIR-1251/1251 (3.1\%)   \\
8  & MIR-326/326 (3.5\%)     & MIR-106B/106B* (3.2\%)  & MIR-206/206 (2.2\%)     & MIR-338/3P (2.8\%)      \\
9  & MIR-105-1/105 (3.3\%)   & MIR-205/205* (3.1\%)    & MIR-101-1/101 (1.9\%)   & MIR-205/205* (2.4\%)    \\
10 & MIR-664/664 (3.1\%)     & MIR-326/326 (2.5\%)     & MIR-30D/30D (1.9\%)     & MIR-126/126 (2.0\%)     \\
11 & MIR-3200/3P (2.8\%)     & MIR-145/145 (2.4\%)     & MIR-4326/4326 (1.8\%)   & MIR-99/99A (1.9\%)      \\
12 & MIR-205/205* (2.4\%)    & MIR-105-1/105 (2.4\%)   & MIR-25/25 (1.8\%)       & MIR-30D/30D (1.9\%)     \\
13 & MIR-222/222 (1.9\%)     & MIR-451/451 (2.0\%)     & LET-7E/7E* (1.8\%)      & MIR-944/944 (1.7\%)     \\
14 & MIR-451/451 (1.8\%)     & MIR-31/31* (1.9\%)      & MIR-24-1/24 (1.8\%)     & MIR-454/454 (1.7\%)     \\
15 & MIR-106B/106B* (1.6\%)  & \textbf{CN\_CLUSTER} (1.9\%) & MIR-15A/15A* (1.7\%)    & MIR-24-1/24 (1.6\%)     \\
16 & MIR-3614/5P (1.6\%)     & MIR-642/642A (1.9\%)    & MIR-20B/20B* (1.6\%)    & MIR-15A/15A* (1.5\%)    \\
17 & MIR-101-1/101 (1.5\%)   & MIR-3200/3P (1.8\%)     & MIR-15B/15B (1.6\%)     & MIR-20B/20B* (1.5\%)    \\
18 & MIR-32/32* (1.5\%)      & MIR-664/664 (1.7\%)     & MIR-874/874 (1.6\%)     & MIR-874/874 (1.5\%)     \\
19 & MIR-200A/200A (1.4\%)   & MIR-345/345 (1.4\%)     & MIR-484/484 (1.5\%)     & MIR-582/3P (1.5\%)      \\
20 & MIR-425/425* (1.4\%)    & MIR-190B/190B (1.3\%)   & MIR-205/205* (1.5\%)    & MIR-4326/4326 (1.5\%)   \\ \hline
\end{tabular}
\end{table}

Several patterns are noteworthy:

\begin{itemize}
    \item \textbf{Basal-like}: MIR-577 dominates with~$\sim$59\% of total gain under miRNA-only
    and $\sim$57\% with clinical data, suggesting it is a highly specific, singular discriminative
    signal. MIR-934 and MIR-135B are the next strongest contributors.

    \item \textbf{HER2-enriched}: The importance profile is more distributed (top feature
    MIR-30/30A accounts for only $\sim$15\% of gain), consistent with the lower overall
    classification performance. With clinical data, \texttt{HER2\_STATUS} enters the top 10,
    confirming the complementary role of the clinical receptor status.

    \item \textbf{Luminal A}: Gain is distributed across many features (top feature contributes
    only $\sim$5\%), suggesting the absence of a dominant singular marker and a more diffuse
    miRNA signature for this subtype.

    \item \textbf{Luminal B}: Similarly distributed gain profile, with the top 20 features
    accounting for only $\sim$50\% of total gain -- the lowest concentration ratio across all
    subtypes, reflecting the heterogeneous nature of Luminal~B.
\end{itemize}


% ============================================================
\subsection{Statistical Corroboration via ANOVA F-Test}
\label{sec:anova-bridge}
% ============================================================
 
The feature importances derived from XGBoost in the previous section are, by their nature,
\textit{model-dependent}: they reflect the contribution of each feature within a specific
learned ensemble, under a specific training regime. A high gain score tells us that a
feature was useful to \textit{this} model, but it does not, on its own, constitute
statistical evidence that the feature's expression genuinely differs across subtypes at the
population level. The two concepts (discriminative utility and statistical significance)
are related but not equivalent, and conflating them would weaken any biological conclusions
drawn from the analysis.
 
This is the fundamental motivation for introducing the ANOVA F-test at this stage. Unlike
XGBoost, ANOVA is entirely model-agnostic: it evaluates each feature independently of any
classifier, asking a direct statistical question: \textbf{does the mean expression of this miRNA
differ significantly between patients of a given subtype and all others?} A feature that
ranks highly under ANOVA is not ``important to a model''; it is \textbf{statistically
distinguishable at the population level}, under the well-defined assumptions of analysis
of variance. This distinction is what gives the ANOVA results their evidential value here:
they provide a source of confirmation that operates through an entirely different lens.
 
When a miRNA is selected as important by XGBoost \textit{and} identified as statistically
significant by ANOVA, the two confirmations are independent. The model did not influence
the F-scores, and the F-scores did not influence the model. It is precisely this
independence that makes the intersection meaningful: a feature that survives both filters
is not an artefact of one method's assumptions or luck, but a signal robust enough to emerge
from two fundamentally different analytical paradigms.
 
Given this, for each subtype $s \in \{\text{Basal-like}, \text{HER2-enriched}, \text{Luminal~A},
\text{Luminal~B}\}$, the analysis was framed as a binary OvR problem, consistent with
the XGBoost experiments. The F-statistic for each feature $j$ is defined as the ratio
of between-group to within-group variance:
 
\begin{equation}
F_j = \frac{n_1\,(\bar{x}_{j,1} - \bar{x}_j)^2 \;+\; n_0\,(\bar{x}_{j,0} - \bar{x}_j)^2}
           {\displaystyle\sum_{k \in \{0,1\}} \sum_{i:\, y_i^{(s)}=k} (x_{ij} - \bar{x}_{j,k})^2 \,/\, (n-2)}
\label{eq:anova}
\end{equation}
 
\noindent where $n_1$ and $n_0$ are the sizes of the positive and negative groups,
$\bar{x}_{j,k}$ is the mean expression of feature $j$ in group $k$, and $\bar{x}_j$
is the overall mean. A large $F_j$ indicates that the expression of feature $j$
varies more \textit{between} the two groups than it does \textit{within} them -
the defining criterion of a statistically discriminative marker.
 
All features were standardised via \texttt{StandardScaler} prior to computing F-scores,
ensuring that differences in expression scale across miRNAs do not artificially inflate
or deflate individual F-statistics. Feature selection was implemented using
\texttt{SelectKBest(f\_classif)} from scikit-learn, embedded in a pipeline with a
downstream XGBoost classifier. This pipeline structure ensured that feature selection
was performed exclusively on training data at each cross-validation fold, with no
information leakage from the test set. The optimal number of features $k$ was
determined per subtype as the value maximising the cross-validated macro F1-score.
Additionally, features were added incrementally in decreasing order of F-score, and the
resulting classification performance trajectory was recorded: revealing both
\textit{how many} features are needed and \textit{which individual features}
contribute most to the discriminative signal.
 
\begin{figure}[h]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{f1_score_vs_number_of_features_anova_without.png}
        \caption{miRNA only}
        \label{fig:f1_mirna}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{f1_score_vs_number_of_features_anova_with.png}
        \caption{miRNA + clinical}
        \label{fig:f1_mirna_clinical}
    \end{subfigure}
    \caption{F1-score (10-fold CV) as a function of the number of features $k$ for each breast cancer subtype, under two feature configurations. Stars indicate the optimal $k$ selected for each subtype.}
    \label{fig:f1_vs_features}
\end{figure}
 
The results reveal a striking gradient across subtypes \textbf{that directly mirrors the XGBoost
findings}. For Basal-like, classification performance saturates rapidly at around $k \approx 10$
(F1~$\approx 0.92$), with MIR-934, MIR-577, and MIR-135B alone accounting for the bulk of
the gain, their F-scores of 386, 358, and 292 respectively are an order of magnitude
above those of any other subtype's top features, reflecting an exceptionally strong and
concentrated statistical signal. For HER2-enriched, by contrast, performance continues
to improve across the full tested range of $k \leq 50$, and the top F-scores barely
exceed 50 - confirming, through a completely independent lens, what XGBoost already
suggested: this subtype lacks a small dominant set of miRNA markers. Luminal~A reaches
its performance ceiling around $k = 20$ (F1~$\approx 0.73$), while Luminal~B exhibits
the weakest and most volatile trajectory of all, with top F-scores around 34 - further
evidence that its molecular boundary is diffuse and that no individual miRNA cleanly
separates it from adjacent subtypes.
 
The consistency of this picture across two independent methods is not incidental. It is
the convergence this work relies upon to move from model-derived feature rankings to
biologically grounded candidate miRNA panels, as formalised in the following section.

With both the XGBoost gain importances and the ANOVA F-scores computed independently, we now
bring them together to identify the \textbf{doubly-validated miRNA panels} for each subtype,
as formalised in Section~\ref{sec:validation-hypothesis}.

\todo{arranjar aqui uma forma de representar a interseção dos resultados do ANOVA com o XGBoost sem ficarmos com tabelas gigantes - Fazemos aquele diagrama de vern ou deixamos isso para último como cartada final?}

% Table~\ref{tab:intersections} presents, for each subtype and each feature configuration, the
% features that appear in both the ANOVA top-ranked list (at the optimal $k$) and the XGBoost
% top-20 gain ranking.

% Tables~\ref{tab:intersect-basal}--\ref{tab:intersect-lumb} present, for each subtype
% and each feature configuration, every feature that appears in either the ANOVA top-ranked
% list (at the subtype-specific optimal $k$) or the XGBoost top-20 gain ranking. Rows
% highlighted in \colorbox{validated}{\textcolor{validatedtext}{green}} are
% \textbf{doubly-validated}: they were independently selected by both methods. Rows in
% \colorbox{clinicalfill}{\textcolor{clinicaltext}{amber}} denote clinical features.
% Unmarked rows are single-method selections only.

% % =====================================================================
% % TABLE 1 — BASAL-LIKE
% % =====================================================================
% \begin{table}[t]
% \centering
% \caption{Feature intersection for \textbf{Basal-like} (OvR binary classification).
% ANOVA F-scores and XGBoost gain percentages are reported for each condition.
% Doubly-validated features appear in both methods independently.}
% \label{tab:intersect-basal}
% \setlength{\tabcolsep}{5pt}
% \renewcommand{\arraystretch}{1.25}
 
% \begin{tabular}{l rr rr}
% \toprule
% & \multicolumn{2}{c}{\textbf{ANOVA}} & \multicolumn{2}{c}{\textbf{XGBoost}} \\
% \cmidrule(lr){2-3}\cmidrule(lr){4-5}
% \textbf{Feature} & Rank & F-score & Rank & Gain (\%) \\
% \midrule
% \multicolumn{5}{l}{\textit{miRNA only}} \\
% \midrule
% \rowcolor{validated}\textcolor{validatedtext}{MIR-934/934}     & 1  & 386.11 & 3  & 8.50  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-577/577}     & 2  & 358.03 & 1  & 59.22 \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-135B/135B*}  & 3  & 291.92 & 2  & 9.45  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-190B/190B}   & 4  & 204.94 & 4  & 4.29  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-455/5P}      & 8  & 134.45 & 5  & 2.76  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-505/505}     & 10 & 124.91 & 7  & 1.86  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-18A/18A*}    & 4  & 214.21 & 19 & 0.45  \\
% MIR-106B/106B*  & 5  & 117.62 & --- & ---   \\
% MIR-18B/18B*    & 6  &  76.65 & --- & ---   \\
% MIR-20A/20A*    & 9  & 119.19 & --- & ---   \\
% \midrule
% \multicolumn{5}{l}{\textit{miRNA + clinical}} \\
% \midrule
% \rowcolor{validated}\textcolor{validatedtext}{MIR-934/934}      & 1  & 386.11 & 3  & 7.73  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-577/577}      & 2  & 358.03 & 1  & 56.86 \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-135B/135B*}   & 3  & 291.92 & 2  & 10.49 \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-190B/190B}    & 4  & 204.94 & 5  & 3.79  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-455/5P}       & 9  & 134.45 & 6  & 3.37  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-505/505}      & 11 & 124.91 & 7  & 1.92  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-519A-1/519A*} & 13 & 135.26 & 14 & 0.77  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-20A/20A*}     & 8  & 119.19 & 19 & 0.56  \\
% \rowcolor{clinicalfill}\textcolor{clinicaltext}{ER\_STATUS}     & 10 & 256.72 & ---& ---   \\
% MIR-106B/106B*  & 6  & 117.62 & --- & ---   \\
% \bottomrule
% \end{tabular}
% \end{table}
 
% % =====================================================================
% % TABLE 2 — HER2-ENRICHED
% % =====================================================================
% \begin{table}[htbp]
% \centering
% \caption{Feature intersection for \textbf{HER2-enriched} (OvR binary classification).}
% \label{tab:intersect-her2}
% \setlength{\tabcolsep}{5pt}
% \renewcommand{\arraystretch}{1.25}
 
% \begin{tabular}{l rr rr}
% \toprule
% & \multicolumn{2}{c}{\textbf{ANOVA}} & \multicolumn{2}{c}{\textbf{XGBoost}} \\
% \cmidrule(lr){2-3}\cmidrule(lr){4-5}
% \textbf{Feature} & Rank & F-score & Rank & Gain (\%) \\
% \midrule
% \multicolumn{5}{l}{\textit{miRNA only}} \\
% \midrule
% \rowcolor{validated}\textcolor{validatedtext}{MIR-30/30A}      & 3  & 40.36 & 1  & 15.20 \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-192/192*}    & 5  & 37.88 & 5  & 6.09  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-184/184}     & 4  & 26.99 & 20 & 1.70  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-187/187}     & 14 & 35.39 & 7  & 3.71  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-2115/2115*}  & 1  & 54.22 & 14 & 2.38  \\
% MIR-345/345     & 17 & 22.53 & --- & ---   \\
% MIR-181D/181D   & 8  & 11.24 & --- & ---   \\
% MIR-497/497*    & 13 & 17.32 & --- & ---   \\
% MIR-3150B/3P    & ---& ---   & 6  & 4.84  \\
% MIR-101/101     & 5  & 13.43 & --- & ---   \\
% \midrule
% \multicolumn{5}{l}{\textit{miRNA + clinical}} \\
% \midrule
% \rowcolor{clinicalfill}\textcolor{clinicaltext}{HER2\_STATUS}   & 1  & 76.90 & 10 & 2.85  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-3150B/3P}    & 2  & 37.55 & 2  & 9.84  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-184/184}     & 4  & 26.99 & 12 & 2.55  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-497/497*}    & 6  & 17.32 & 11 & 2.74  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-30C/3P}      & 9  & 20.79 & 14 & 2.12  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-187/187}     & 10 & 35.39 & 7  & 4.28  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-30/30A}      & 14 & 40.36 & 1  & 10.65 \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-217/217}     & 19 & 11.83 & 19 & 1.73  \\
% \rowcolor{clinicalfill}\textcolor{clinicaltext}{PR\_STATUS}    & 5  & 17.39 & ---& ---   \\
% MIR-345/345     & 3  & 22.53 & --- & ---   \\
% \bottomrule
% \end{tabular}
% \end{table}
 
% % =====================================================================
% % TABLE 3 — LUMINAL A
% % =====================================================================
% \begin{table}[htbp]
% \centering
% \caption{Feature intersection for \textbf{Luminal A} (OvR binary classification).}
% \label{tab:intersect-luma}
% \setlength{\tabcolsep}{5pt}
% \renewcommand{\arraystretch}{1.25}
 
% \begin{tabular}{l rr rr}
% \toprule
% & \multicolumn{2}{c}{\textbf{ANOVA}} & \multicolumn{2}{c}{\textbf{XGBoost}} \\
% \cmidrule(lr){2-3}\cmidrule(lr){4-5}
% \textbf{Feature} & Rank & F-score & Rank & Gain (\%) \\
% \midrule
% \multicolumn{5}{l}{\textit{miRNA only}} \\
% \midrule
% \rowcolor{validated}\textcolor{validatedtext}{MIR-1301/1301}   & 2  & 85.14 & 4  & 4.61  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-15B/15B*}    & 4  & 71.69 & 5  & 4.00  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-19A/19A}     & 20 & 76.81 & 3  & 4.79  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-106B/106B*}  & 7  & 68.14 & 15 & 1.61  \\
% MIR-942/942     & 1  & 102.90& --- & ---   \\
% MIR-590/3P      & 5  & 76.03 & --- & ---   \\
% MIR-18A/18A*    & 3  & 70.57 & --- & ---   \\
% MIR-30/30A      & ---& ---   & 1  & 5.14  \\
% MIR-501/3P      & ---& ---   & 2  & 4.82  \\
% \midrule
% \multicolumn{5}{l}{\textit{miRNA + clinical}} \\
% \midrule
% \rowcolor{validated}\textcolor{validatedtext}{MIR-1301/1301}   & 2  & 85.14 & 5  & 4.06  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-15B/15B*}    & 5  & 71.69 & 7  & 3.61  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-106B/106B*}  & 8  & 68.14 & 8  & 3.15  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-105-1/105}   & 18 & 41.56 & 12 & 2.36  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-30A/30A*}    & 20 & 40.86 & 1  & 5.38  \\
% MIR-942/942     & 1  & 102.90& --- & ---   \\
% MIR-590/3P      & 6  & 76.03 & --- & ---   \\
% MIR-210/210     & 7  & 44.84 & --- & ---   \\
% MIR-19A/19A     & ---& ---   & 2  & 4.98  \\
% MIR-501/3P      & ---& ---   & 3  & 4.94  \\
% \bottomrule
% \end{tabular}
% \end{table}
 
% % =====================================================================
% % TABLE 4 — LUMINAL B
% % =====================================================================
% \begin{table}[htbp]
% \centering
% \caption{Feature intersection for \textbf{Luminal B} (OvR binary classification).
% The sparsity of doubly-validated features in the miRNA-only condition reflects
% the known molecular heterogeneity of this subtype.}
% \label{tab:intersect-lumb}
% \setlength{\tabcolsep}{5pt}
% \renewcommand{\arraystretch}{1.25}
 
% \begin{tabular}{l rr rr}
% \toprule
% & \multicolumn{2}{c}{\textbf{ANOVA}} & \multicolumn{2}{c}{\textbf{XGBoost}} \\
% \cmidrule(lr){2-3}\cmidrule(lr){4-5}
% \textbf{Feature} & Rank & F-score & Rank & Gain (\%) \\
% \midrule
% \multicolumn{5}{l}{\textit{miRNA only}} \\
% \midrule
% \rowcolor{validated}\textcolor{validatedtext}{MIR-99/99A}      & 4  & 34.53 & 7  & 2.77  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-15A/15A}     & 9  & 10.32 & 15 & 1.69  \\
% MIR-99A/99A*    & 1  & 34.53 & --- & ---   \\
% MIR-887/887     & 2  & 25.71 & --- & ---   \\
% MIR-193B/3P     & 5  & 10.29 & --- & ---   \\
% MIR-3677/3P     & ---& ---   & 1  & 5.09  \\
% MIR-190B/190B   & ---& ---   & 2  & 4.90  \\
% MIR-126/126     & ---& ---   & 3  & 4.13  \\
% MIR-135B/135B*  & 15 & 13.10 & --- & ---   \\
% MIR-934/934     & 16 & 16.68 & --- & ---   \\
% \midrule
% \multicolumn{5}{l}{\textit{miRNA + clinical}} \\
% \midrule
% \rowcolor{validated}\textcolor{validatedtext}{MIR-99/99A}      & 1  & 34.53 & 11 & 1.93  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-205/205*}    & 2  & 24.22 & 9  & 2.41  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-338/3P}      & 19 & 8.96  & 8  & 2.76  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-454/454}     & 15 & 10.56 & 14 & 1.69  \\
% \rowcolor{validated}\textcolor{validatedtext}{MIR-15A/15A}     & 18 & 10.32 & 16 & 1.54  \\
% \rowcolor{clinicalfill}\textcolor{clinicaltext}{ER\_STATUS}    & 13 & 32.17 & ---& ---   \\
% MIR-193B/3P     & 3  & 10.29 & --- & ---   \\
% MIR-107-10/107  & 4  & 14.49 & --- & ---   \\
% MIR-190B/190B   & ---& ---   & 1  & 5.49  \\
% MIR-3677/3P     & ---& ---   & 2  & 4.95  \\
% \bottomrule
% \end{tabular}
% \end{table}
 

% \subsection{Summary of Doubly-Validated Features}

% The number of doubly-validated features per subtype and condition is summarised below:

% \begin{center}
% \begin{tabular}{lcc}
% \hline
% \textbf{Subtype} & \textbf{miRNA only} & \textbf{miRNA + clinical} \\
% \hline
% Basal-like       & 7 & 8 \\
% HER2-enriched    & 5 & 8 \\
% Luminal A        & 4 & 5 \\
% Luminal B        & 2 & 5 \\
% \hline
% \end{tabular}
% \end{center}

% The consistent increase in validated features when clinical data is added -- especially for
% HER2-enriched (+3) and Luminal B (+3) -- is itself a finding: it quantifies the
% contribution of clinical covariates not just to classification accuracy, but to the
% identifiability of subtype-specific molecular markers.

% \paragraph{Basal-Like}

% The three features \textbf{MIR-577, MIR-934, and MIR-135B} are doubly validated with
% the highest confidence: they rank in the top~3 in both ANOVA (F-scores of 358, 386, and
% 292 respectively) and XGBoost gain. MIR-577's dominance in XGBoost ($\sim 59\%$ gain) paired
% with its high ANOVA F-score positions it as the \textbf{strongest candidate discriminative
% marker for Basal-like}. MIR-190B, MIR-455-5p, and MIR-505 complete the core doubly-validated
% panel. With clinical data, MIR-519A and MIR-20A enter the intersection. Notably,
% \texttt{ER\_STATUS} appears in ANOVA (F-score 256.72) but not in the XGBoost top 20,
% suggesting it carries individual statistical signal but is not the most useful discriminative
% feature in the multivariate context -- consistent with the fact that Basal-like tumours are
% reliably characterised as ER-negative, making the clinical feature partially redundant when
% the miRNA signature is already present.

% \paragraph{HER2-Enriched}

% Without clinical data, only 5 features are doubly validated, reflecting the difficulty of
% this subtype. With clinical data, \textbf{HER2\_STATUS} tops the ANOVA ranking (F-score 76.90)
% and also appears in the XGBoost top 10, making it the most strongly validated feature for
% this subtype -- and the only clinical feature to achieve double validation. This finding
% strongly supports the clinical intuition that HER2 receptor status is the defining feature of
% this subtype. Among miRNAs, \textbf{MIR-30/30A and MIR-3150B-3p} are the most robustly
% validated, appearing in both lists under both conditions.

% \paragraph{Luminal A}

% The four doubly-validated features without clinical data -- \textbf{MIR-1301, MIR-15B,
% MIR-19A, and MIR-106B} -- form a compact panel with moderate but consistent signal.
% A notable discrepancy is observed: \textbf{MIR-942} achieves the highest ANOVA F-score
% for this subtype (102.90) and generates the largest single-feature F1 improvement (+0.56),
% yet does not appear in the XGBoost top 20. This may indicate that MIR-942's discriminative
% power is primarily marginal -- it differs strongly in mean expression between Luminal~A and
% other subtypes -- but in the presence of other features within the model, its independent
% contribution is reduced (possibly due to correlation with other included features). This
% discrepancy is itself an informative finding: it illustrates the complementarity of the two
% methods and cautions against relying exclusively on either one.

% \paragraph{Luminal B}

% Luminal B presents the weakest intersection, with only 2 doubly-validated features
% (\textbf{MIR-99/99A and MIR-15A}) under the miRNA-only condition. The overall low ANOVA
% F-scores (top feature F-score~$\approx 34$, compared to 386 for Basal-like) indicate that
% no single miRNA differs dramatically in mean expression between Luminal~B and other subtypes.
% With clinical data, 5 features are validated, including \textbf{MIR-99/99A} as the most
% consistently appearing feature across both methods and both conditions -- making it the
% strongest candidate marker for Luminal~B. The finding that ER\_STATUS appears in ANOVA
% (F-score 32.17) but not in XGBoost top 20 is consistent with Luminal~B's hormonal status
% being shared with Luminal~A, reducing its subtype-specific discriminative power.

% \subsection{Cross-Subtype Observations}

% Several miRNAs appear in the doubly-validated panels of more than one subtype, which is
% biologically expected given the partial overlap between PAM50 categories:

% \begin{itemize}
%     \item \textbf{MIR-190B} appears in both Basal-like and Luminal~B panels, suggesting
%     it may reflect a more general characteristic of aggressive or basal-program tumours.
%     \item \textbf{MIR-577} and \textbf{MIR-135B} appear in Basal-like with high dominance
%     but also with lower importance scores in Luminal~B, suggesting subtype-specific
%     expression gradients rather than strict on/off expression.
% \end{itemize}

% [SUGGESTION: Cross-reference any of the identified doubly-validated miRNAs with published
% literature on breast cancer miRNA biomarkers -- particularly MIR-577, MIR-135B, MIR-30/30A,
% and MIR-99 -- to assess whether the computational findings are consistent with known
% biological roles. This contextualisation is essential for the biological discussion section
% of the thesis.]

% \subsection{Limitations and Caveats}

% Several limitations of this analysis deserve explicit acknowledgement:

% \begin{enumerate}
%     \item The dataset comprises only 244 samples, which limits statistical power -
%     particularly for minority subtypes (HER2-enriched: $n=29$). Validation on a larger,
%     independent cohort is necessary before the identified panels can be considered clinically
%     actionable.

%     \item The two-level validation framework is confirmatory, not exploratory: it increases
%     confidence in features already appearing in both ranked lists, but cannot rule out relevant
%     features that are highly correlated with, and therefore displaced by, other selected features
%     (redundancy effects, especially in XGBoost gain).

%     \item ANOVA's assumptions (normality, homoscedasticity) may not hold strictly for all
%     miRNA features; however, the large sample sizes within each group provide some robustness
%     via the central limit theorem.
% \end{enumerate}

Despite these limitations, the \textbf{convergence of two fundamentally different methods}
onto a consistent set of features, under both data configurations and across a subtype
as genomically distinct as Basal-like, provides a degree of confidence in the identified
panels that neither method alone could establish.

%=============================================================
% ============================================================
%  UNSUPERVISED APPROACH — K-MEANS CLUSTERING
%  Thesis: Learning to map miRNAs to subtypes of breast cancer
%         using Machine Learning
% ============================================================

\section{Group-Based Subtyping}
\label{sec:unsupervised}

The previous section addressed the problem of breast cancer subtype classification through
a supervised learning lens: labelled samples were used to train models that learn an explicit
decision boundary between the four PAM50 subtypes. While this is the conventional formulation
for clinical classification tasks, it rests on a fundamental assumption that the labels are
reliable, complete, and the only relevant structure in the data.

This section sets those labels aside, thus transforming the core question we ask:
\textbf{does the miRNA expression data itself, without any guidance from clinical annotations,
organise into groups that reflect the known biological subtypes?} If such structure exists, it
suggests that the subtype-discriminating signal is intrinsic to the transcriptomic landscape and
not merely an artefact of how patients were clinically classified. Moreover, for any cluster
that aligns strongly with a known subtype, we can identify which miRNAs drive that alignment 
and complement the analysis of the supervised models.

% ----------------------------------------------------------
% \subsection{Background: Unsupervised Learning and K-Means}
% \label{sec:unsupervised:background}

% Unsupervised learning refers to a family of methods that operate on unlabelled data, seeking
% to discover latent structure without explicit supervision \cite{hastie2009elements}. The
% prototypical task is \textit{clustering}: partitioning a dataset into groups such that samples
% within a group are more similar to each other than to samples in other groups. Unlike supervised
% classification, there is no ground truth to optimise against during training; evaluation relies
% either on internal geometric measures or on post-hoc comparison with external labels when
% available.

% K-Means is among the most widely used clustering algorithms. Originally
% introduced by \citeauthor{macqueen1967some} \cite{macqueen1967some}, it partitions $n$ samples
% into $K$ clusters by iteratively minimising the within-cluster sum of squared Euclidean
% distances (inertia):

% \begin{equation}
%     \mathcal{L} = \sum_{k=1}^{K} \sum_{\mathbf{x}_i \in C_k}
%     \left\| \mathbf{x}_i - \boldsymbol{\mu}_k \right\|^2
%     \label{eq:kmeans_objective}
% \end{equation}

% \noindent where $C_k$ denotes the set of samples assigned to cluster $k$ and
% $\boldsymbol{\mu}_k$ is the centroid of that cluster. The algorithm alternates between two
% steps: \\

% \begin{enumerate}
%     \item assigning each sample to its nearest centroid;
%     \item recomputing each centroid as the mean of its assigned samples
% \end{enumerate}

% Unsupervised learning methods have been applied to genomic and transcriptomic cancer data since
% the early studies of molecular classification. Notably, hierarchical clustering on gene
% expression microarray data was used to identify intrinsic breast cancer subtypes in the landmark
% work of \citeauthor{perou2000molecular} \cite{perou2000molecular}, demonstrating that
% transcriptomic profiles alone, without clinical labels, can recover biologically meaningful
% tumour classes.

% ----------------------------------------------------------
\subsection{Why This Approach Is Non-Conventional}
\label{sec:unsupervised:nonconventional}

Applying unsupervised learning to a problem that has well-defined class labels is, by most
standards, a non-conventional choice. Standard practice in breast cancer subtype research, 
or in other cancer research areas, would typically involve using the labels (in 
this case the PAM50 gene signature) to train a supervised classifier and then
treat subtype assignment as a supervised classification problem leveraging the ground truth (similiar to \cite{ml_gastric_Azari2023}).

The unconventional nature of this approach is deliberate. Rather than asking \textbf{"can we
predict the subtype of a patient?"}, we ask \textbf{"does the data naturally group itself in a
way that corresponds to known subtypes?"}. These are fundamentally different questions. The first
is a performance question; the second is a structural one. A high-performing supervised
classifier may exploit subtle decision boundaries that have no clear biological interpretation.
A cluster that achieves high purity with respect to a known subtype, by contrast, asserts
that the subtype occupies a geometrically coherent and isolated region of the miRNA expression
space, making it a stronger biological claim.

Furthermore, this framing makes the analysis robust to label noise: the clustering algorithm
never sees the labels, so any correspondence between clusters and subtypes that emerges is
entirely data-driven.

% ----------------------------------------------------------
\subsection{Implementation}
\label{sec:unsupervised:implementation}

\subsubsection{Dataset and Experimental Conditions}

Experiments were conducted on the small cohort ($n = 244$ samples). Two experimental
conditions were evaluated in parallel:

\begin{itemize}
    \item \textbf{miRNA only:} 442 miRNA expression features, after preprocessing.
    \item \textbf{miRNA + clinical:} 462 features, combining miRNA expression with clinical
    covariates.
\end{itemize}

Running both conditions through the same pipeline serves a concrete purpose: it allows
us to assess whether miRNA expression alone carries sufficient structure to recover
biologically meaningful clusters, and whether clinical data strengthens or dilutes that
signal. All conclusions are drawn in parallel for both conditions so that differences
can be attributed to the feature set rather than to any other methodological choice.

While K-Means minimises Euclidean distances in feature space, making it sensitive to feature scale.
A feature with values in the range $[0, 1000]$ will dominate distance calculations over a
feature in $[0, 1]$, regardless of their relative biological importance.
Accordingly, all features were standardised using \textit{StandardScaler}, transforming each
feature to zero mean and unit variance prior to clustering.

No correlation-based redundancy removal was applied. Although correlated features may inflate
the weight of certain directions in Euclidean space, removing
them based on pairwise correlation would risk discarding biologically relevant miRNAs which, 
in our case, we cannot afford. This would mean that a miRNA identified as a discriminative 
feature in the supervised analysis could be
silently removed as a correlated redundant, breaking the cross-comparison between the two
approaches. Retaining the full feature space ensures that the post-hoc feature importance
analysis (Section~\ref{sec_unsupervised_importance}) operates on the same miRNA vocabulary as
the supervised section, enabling a direct comparison of which miRNAs are flagged by each
paradigm. The presence of correlated features is acknowledged as a methodological constraint.

\subsubsection{Number of clusters}

The number of clusters $K$ was set to 4, corresponding to the four known PAM50 breast
cancer subtypes (Basal-Like, HER2-Enriched, Luminal A, and Luminal B): since the
ultimate goal is to assess whether the unsupervised structure aligns with known biology,
fixing $K$ to match the number of subtypes is the only choice that makes the external
evaluation metrics meaningful. Figure~\ref{fig:kmeans_k_selection_combined} shows the
diagnostic curves for both conditions. In both cases, the elbow method produces a
gradual, continuously declining inertia curve with no discernible inflection point at
any $K$. The silhouette score peaks at $K = 2$ ($\approx 0.115$ for miRNA-only;
$\approx 0.111$ for miRNA + clinical) and decreases monotonically thereafter, reaching
$\approx 0.069$ at $K = 4$ in both conditions. The two curves are strikingly similar,
indicating that neither feature set produces a geometry that the data-driven criteria
can resolve into cleanly separated clusters. Consequently, neither curve provides
unambiguous support for $K = 4$, reinforcing that the choice must be grounded in domain
knowledge.

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{\textwidth}
        \centering
        \includegraphics[width=0.9\textwidth]{miRNA_only_k_selection.png}
        \caption{miRNA-only condition.}
        \label{fig:k_sel_mirna_only}
    \end{subfigure}
    
    \vspace{2em}

    \begin{subfigure}[b]{\textwidth}
        \centering
        \includegraphics[width=0.9\textwidth]{miRNA_and_clinical_k_selection.png}
        \caption{miRNA and clinical data condition.}
        \label{fig:k_sel_mirna_clinical}
    \end{subfigure}

    \caption{Elbow method (inertia vs.\ $K$, left) and mean silhouette score vs.\ $K$
    (right) for both experimental conditions. The red dashed line marks the chosen value
    $K = 4$. Both conditions produce curves of the same shape, with no clear elbow and
    a silhouette peak at $K = 2$.}
    \label{fig:kmeans_k_selection_combined}
\end{figure}

This choice is justified on two grounds. First, purity
evaluated against the four PAM50 classes is only meaningful when the number of clusters
matches the number of classes: setting $K = 3$ or $K = 5$ would inevitably create
clusters that either merge or split subtypes, making any purity comparison with the
supervised results ambiguous. Second, one of the central findings of the supervised
section was that HER2-enriched and Luminal B are the hardest subtypes to separate. Setting
$K = 4$ preserves that boundary, allowing us to directly test whether K-Means, operating
without labels, can recover it from expression data alone.

\todo{Esta subsecção pode ser retirada se acharmos que a tese está muito extensa: o valor que trás não é assim tanto}
\subsubsection{Model Fitting}
K-Means was fitted using the K-Means++ initialisation strategy, which selects initial
centroids with probability proportional to their squared distance from already-chosen
centroids. Compared to purely random initialisation, this approach reduces the risk of
converging to poor local minima and generally accelerates convergence, making it
particularly advantageous in high-dimensional spaces where the inertia landscape contains
numerous local optima.

To further mitigate sensitivity to initialisation, $n\_init = 10$ independent random
restarts were performed, each starting from a different K-Means++ initialised set of
centroids. The solution yielding the lowest inertia across all restarts was retained as
the final model. Convergence within each restart was determined by scikit-learn's default
stopping criteria: a centroid shift tolerance of $10^{-4}$ between successive iterations,
with a maximum of 300 iterations permitted if the tolerance threshold is not reached.
A fixed random seed (\texttt{random\_state} $= 42$) was set throughout to ensure full
reproducibility of both the initialisation and the restart sequence.

Figure~\ref{fig:kmeans_silhouette_combined} shows the per-sample silhouette plots at
$K = 4$ for both conditions. The mean silhouette score is low in both cases ($0.069$
for miRNA-only; $0.068$ for miRNA + clinical), consistent with the absence of a clear
elbow and confirming that the four clusters do not form geometrically compact,
well-separated regions in the standardised feature space. This is not unexpected: K-Means
assumes convex, isotropic clusters of similar size, an assumption that high-dimensional
transcriptomic data rarely satisfies. The low silhouette is therefore a property of the
data geometry, not necessarily evidence that no biological structure exists.

Despite this similarity in aggregate score, the two conditions differ in one important
respect. In the miRNA-only setting, all four clusters produce predominantly positive
silhouette coefficients with broadly uniform band widths, indicating that samples are
consistently --- if weakly --- assigned to their nearest centroid. In the miRNA +
clinical condition, Cluster 0 exhibits a substantial proportion of negative coefficients,
suggesting that a non-trivial number of samples are geometrically closer to a
neighbouring cluster than to their own. This asymmetry is an early indication that the
addition of clinical features introduces heterogeneity that K-Means cannot resolve at
$K = 4$, a hypothesis that will be confirmed by the external evaluation metrics in the
following section.

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{0.85\textwidth}
        \centering
        \includegraphics[width=\textwidth]{miRNA_only_silhouette_plot.png}
        \caption{miRNA-only condition.}
        \label{fig:silhouette_mirna_only}
    \end{subfigure}
    
    \vspace{1em} 

    \begin{subfigure}[b]{0.85\textwidth}
        \centering
        \includegraphics[width=\textwidth]{miRNA_and_clinical_silhouette_plot.png}
        \caption{miRNA and clinical condition.}
        \label{fig:silhouette_mirna_clinical}
    \end{subfigure}

    \caption{Per-sample silhouette plots for $K = 4$ under both experimental conditions.
    Each coloured band corresponds to one cluster. Bars to the right of zero indicate
    well-assigned samples; bars to the left indicate potential misassignments. The red
    dashed line marks the mean silhouette score ($0.069$ for miRNA-only and $0.068$ for
    miRNA + clinical). Note Cluster 0 in the miRNA + clinical condition: it is the only
    cluster in either condition to exhibit a substantial fraction of negative coefficients.}
    \label{fig:kmeans_silhouette_combined}
\end{figure}

% ----------------------------------------------------------

\subsection{Clustering Analysis and Evaluation}
\label{sec:unsupervised:results}

Having fitted the model and characterised the geometry of the resulting partition, we
now turn to the central question of this section: does K-Means, operating entirely
without label information, recover a partition that corresponds to the known PAM50
subtype structure? And does the addition of clinical covariates strengthen or weaken that
correspondence? We address both questions at three levels of increasing granularity:
aggregate evaluation metrics, per-cluster composition, and miRNA feature signatures.
Both experimental conditions are analysed in parallel throughout, so that differences
can be attributed unambiguously to the effect of the feature set.
 
\subsubsection{Cluster Quality Metrics}

The K-Means algorithm was evaluated under two experimental conditions - miRNA expression
features alone (442 features) and miRNA expression combined with clinical covariates (462
features) - both fitted on the small cohort of $n = 244$ samples with $K = 4$ clusters, in
correspondence with the four PAM50 breast cancer subtypes. Given this, 
Table~\ref{tab:kmeans_summary} summarises the four evaluation metrics across both conditions.

\begin{table}[t]
\centering
\caption{K-Means performance summary for $K = 4$ on the small cohort ($n = 244$).}
\label{tab:kmeans_summary}
\begin{tabular}{lrrrrr}
\hline
\textbf{Condition} & \textbf{Features} & \textbf{Silhouette} & \textbf{Purity} & \textbf{ARI} & \textbf{AMI} \\
\hline
miRNA only          & 442 & 0.069 & 0.611 & 0.203 & 0.258 \\
miRNA + clinical    & 462 & 0.068 & 0.598 & 0.192 & 0.222 \\
\hline
\end{tabular}
\end{table}

The external metrics tell an informative story. An overall purity of $0.611$ for the
miRNA-only condition means that $61\%$ of samples are assigned to a cluster where their
true PAM50 subtype is the majority class. Under random assignment of 244 samples into 4
equal-sized clusters, the expected purity would be approximately $0.25$; the observed
value therefore represents a meaningful above-chance alignment between the discovered
partition and the ground-truth labels. The ARI of $0.203$ and AMI of $0.258$ are
similarly modest but non-trivial, both well above the zero baseline expected from a
random partition.

Across all four metrics, the miRNA-only condition consistently outperforms miRNA +
clinical. The differences are small in absolute terms but systematic in direction, with
the AMI showing the largest relative drop ($\approx 14\%$) when clinical features are
added. This consistency points to a clear conclusion: the subtype-discriminating signal
in this cohort resides primarily in the miRNA expression space, and appending clinical
covariates introduces variation that is orthogonal to PAM50 subtype boundaries. Because
K-Means treats all standardised features symmetrically, each clinical variable occupies
one dimension with the same weight as any miRNA, effectively adding independent axes of
variation that pull centroids away from the miRNA-driven structure and dilute the
clustering signal.

% ----------------------------------------------------------
\subsubsection{Cluster Composition}

While aggregate metrics provide a global picture, they do not reveal which subtypes are
correctly isolated and which are conflated. Examining the internal composition of each
cluster answers that question and allows us to identify which PAM50 subtypes produce a
geometrically coherent signal in expression space and which are systematically confused
with one another. An important methodological note before proceeding: K-Means cluster
indices are arbitrary and not comparable across conditions by number alone; two clusters
are considered to correspond to the same biological entity when they share the same
dominant PAM50 subtype. Table~\ref{tab:kmeans_both_composition} and
Figure~\ref{fig:kmeans_purity_heatmaps_combined} present the full composition for both
conditions, while Figure~\ref{fig:kmeans_pie_charts_both} shows the subtype distribution
within each cluster.

\begin{table}[t]
\centering
\caption{Cluster composition for both experimental conditions ($K = 4$, $n = 244$).
Each cell reports the raw count of samples of that subtype in that cluster. The dominant
subtype per cluster is indicated in the rightmost column with its within-cluster purity
in parentheses.}
\label{tab:kmeans_both_composition}
\begin{tabular}{clrrrrl}
\hline
\textbf{Condition} & \textbf{Cluster} & \textbf{Basal} & \textbf{HER2} &
\textbf{Lum.\ A} & \textbf{Lum.\ B} & \textbf{Dominant (purity)} \\
\hline
\multirow{4}{*}{miRNA only}
 & C0 ($n=68$) &  0 &  6 & \textbf{45} & 17 & Lum.\ A (66.2\%) \\
 & C1 ($n=47$) & \textbf{34} &  5 &  1 &  7 & Basal-Like (72.3\%) \\
 & C2 ($n=66$) &  3 &  0 & \textbf{42} & 21 & Lum.\ A (63.6\%) \\
 & C3 ($n=63$) &  4 & 18 & 15 & \textbf{26} & Lum.\ B (41.3\%) \\
\hline
\multirow{4}{*}{miRNA + clinical}
 & C0 ($n=62$) &  0 &  4 & \textbf{43} & 15 & Lum.\ A (69.4\%) \\
 & C1 ($n=65$) &  5 & 18 & 15 & \textbf{27} & Lum.\ B (41.5\%) \\
 & C2 ($n=67$) &  2 &  2 & \textbf{43} & 20 & Lum.\ A (64.2\%) \\
 & C3 ($n=50$) & \textbf{34} &  5 &  2 &  9 & Basal-Like (68.0\%) \\
\hline
\end{tabular}
\end{table}

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{miRNA_only_purity_heatmap.png}
        \caption{miRNA only}
        \label{fig:mirna_purity}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{miRNA_and_clinical_purity_heatmap.png}
        \caption{miRNA + clinical}
        \label{fig:clinical_purity}
    \end{subfigure}
    \caption{Cluster $\times$ Subtype purity heatmaps for both conditions. Each cell
    reports the proportion of samples of that subtype within the cluster. Note that
    cluster indices are not directly comparable across conditions; the same biological
    structure may appear under a different index in each panel.}
    \label{fig:kmeans_purity_heatmaps_combined}
\end{figure}

\todo{figura completamente caótica: como é que vou meter isto sem ocupar uma página inteira de modo a que se veja?}
\begin{figure}[t]
    \centering
    % --- DATASET 1 Section ---
    \textbf{miRNA Only: Cluster Distribution} \\[1ex]    
    \vspace{-0.5cm} % Significant gap between datasets

    \includegraphics[width=0.95\textwidth]{miRNA_only_cluster_subtype.pdf}
    \caption*{Figure 1a: Clusters 0 ($n=68$) and 1 ($n=47$)}
    
    \vspace{-0.5cm} % Significant gap between datasets
    
    \includegraphics[width=0.95\textwidth]{miRNA_only_cluster_subtype2.pdf}
    \caption*{Figure 1b: Clusters 2 ($n=66$) and 3 ($n=63$)}

    \vspace{-2.0cm} 

    % --- DATASET 2 Section ---
    \textbf{miRNA + Clinical Data: Cluster Distribution} \\[1ex]
    \includegraphics[width=0.95\textwidth]{miRNA_clinical_cluster_subtype.pdf}
    \caption*{Figure 2a: Dataset 2 Clusters 0 \& 1}
    
    \vspace{-2.0cm}
    
    \includegraphics[width=0.95\textwidth]{miRNA_clinical_cluster_subtype2.pdf}
    \caption*{Figure 2b: Dataset 2 Clusters 2 \& 3}

    \caption{Comprehensive Comparison of Breast Cancer Subtypes across Two Datasets.}
    \label{fig:kmeans_pie_charts_both}
\end{figure}

At the level of dominant subtypes, the partition recovered by both conditions is
strikingly similar: both produce one Basal-Like-dominant cluster, two Luminal A-dominant
clusters, and one mixed Luminal B / HER2-Enriched cluster. This structural consistency
is itself a meaningful result: it suggests that the four-cluster organisation of the
data is driven primarily by the miRNA expression component, and that the addition of
clinical features does not fundamentally alter the topology of the recovered partition.
The differences between conditions are therefore quantitative rather than qualitative.

\paragraph{The Basal-Like cluster is the most biologically coherent in both conditions.}
In the miRNA-only condition, the Basal-Like cluster (C1) achieves a purity of $72.3\%$,
with 34 out of 47 samples belonging to the Basal-Like subtype. Under the miRNA +
clinical condition, the corresponding cluster (C3) recovers the same 34 Basal-Like
samples but draws in 3 additional non-Basal samples, reducing purity slightly to $68.0\%$
($n=50$). The high coherence of this cluster in both conditions is consistent with the
clinical literature: Basal-Like (triple-negative) tumours are known to exhibit a
markedly distinct molecular profile from hormone-receptor-positive subtypes
\cite{perou2000molecular}, and this distinction is clearly captured by miRNA expression
alone. The small purity drop when clinical features are added is a direct consequence
of the metric dilution described above: a few non-Basal samples whose clinical profile
places them geometrically close to the Basal centroid are drawn into the cluster.

\paragraph{Luminal A is fragmented into two clusters in both conditions.}
Both conditions produce two distinct Luminal A-dominant clusters rather than a single
cohesive one. In the miRNA-only case these are C0 ($66.2\%$ purity, $n=68$) and C2
($63.6\%$ purity, $n=66$); under miRNA + clinical they are C0 ($69.4\%$ purity,
$n=62$) and C2 ($64.2\%$ purity, $n=67$). Both clusters in both conditions show a
secondary Luminal B component and negligible Basal-Like or HER2-Enriched representation.
Rather than being a failure of the clustering, this fragmentation is informative in
itself: Luminal A tumours do not occupy a single compact region in miRNA expression space
but form at least two separable subgroups with distinct centroid profiles. This is
consistent with the supervised analysis, where Luminal A showed heterogeneous behaviour
across feature subsets, and aligns with evidence in the literature of biological
heterogeneity within the Luminal A class \cite{perou2000molecular}.

\paragraph{Luminal B and HER2-Enriched remain systematically mixed in both conditions.}
Both conditions produce one cluster in which Luminal B and HER2-Enriched samples
co-dominate, with near-identical composition: $41.3\%$ Luminal B and $28.6\%$
HER2-Enriched in the miRNA-only condition; $41.5\%$ Luminal B and $27.7\%$
HER2-Enriched in the miRNA + clinical condition. This near-identical mixing across
both conditions confirms that the difficulty of separating these two subtypes is an
intrinsic property of the miRNA expression space, not an artefact of the feature set,
directly echoing the confusion observed in the supervised analysis.

% ----------------------------------------------------------
\subsubsection{Feature Signatures}
\label{sec_unsupervised_importance}

Knowing that each cluster aligns to a dominant PAM50 subtype is only part of the story.
The next question is which miRNAs define each cluster's centroid: that is, which features
deviate most strongly from the global dataset mean within each group. These are the
miRNAs that the unsupervised algorithm implicitly used to separate the data, and
identifying them serves two purposes. First, it provides a biologically interpretable
signature for each cluster. Second, and more importantly in the context of this thesis,
it enables a direct cross-comparison with the discriminative features identified in the
supervised section, allowing us to assess whether both paradigms converge on the same
miRNAs or capture complementary aspects of subtype biology. Both conditions are examined
here so that the stability of the identified signatures can be directly assessed.

Each miRNA is ranked by the absolute deviation of its cluster-level mean expression
from the global cohort mean, expressed in standard deviation units (absolute centroid
z-score). A miRNA that is consistently over or underexpressed in a given cluster
relative to the full cohort receives a high absolute score, marking it as characteristic
of that cluster. This is the canonical way to interpret K-Means centroids, since the
centroid is simply the mean expression profile of the cluster \cite{hastie2009elements}.

Figure~\ref{fig:kmeans_top_features_both} shows the top-20 feature rankings for all
four clusters under both conditions, and Table~\ref{tab:top5_both} summarises the top-5
features per biological cluster type.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.7\textwidth]{sc_unsupervised_top20_c0_mirna.png}
    \caption*{(a) Cluster 0}
    \vspace{0.5em}
    \includegraphics[width=0.7\textwidth]{sc_unsupervised_top20_c1_mirna.png}
    \caption*{(b) Cluster 1}
    \vspace{0.5em}
    \includegraphics[width=0.7\textwidth]{sc_unsupervised_top20_c2_mirna.png}
    \caption*{(c) Cluster 2}
    \vspace{0.5em}
    \includegraphics[width=0.7\textwidth]{sc_unsupervised_top20_c3_mirna.png}
    \caption*{(d) Cluster 3}
    \caption{
        Top-20 absolute centroid z-score feature rankings for each K-Means cluster with miRNA-only features, where
        each panel shows the miRNAs most strongly characteristic of the cluster.
        (Cluster assignments and correspondence are described in Table~\ref{tab:kmeans_both_composition}.)
    }
    \label{fig:sc_kmeans_top_features_mirna_only}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.7\textwidth]{sc_unsupervised_top20_c0_clinical.png}
    \caption*{(a) Cluster 0}
    \vspace{0.5em}
    \includegraphics[width=0.7\textwidth]{sc_unsupervised_top20_c1_clinical.png}
    \caption*{(b) Cluster 1}
    \vspace{0.5em}
    \includegraphics[width=0.7\textwidth]{sc_unsupervised_top20_c2_clinical.png}
    \caption*{(c) Cluster 2}
    \vspace{0.5em}
    \includegraphics[width=0.7\textwidth]{sc_unsupervised_top20_c3_clinical.png}
    \caption*{(d) Cluster 3}
    \caption{
        Top-20 absolute centroid z-score feature rankings for each K-Means cluster with miRNA and clinical features, where
        each panel shows the features that most strongly characterize the cluster
        (Cluster assignments and correspondence are described in Table~\ref{tab:kmeans_both_composition}.)
    }
    \label{fig:sc_kmeans_top_features_mirna_clinical}
\end{figure}

\begin{table}[t]
    \centering
    \caption{Top 5 miRNA features per biological cluster type by absolute centroid z-score,
    for both experimental conditions. Clusters are identified by dominant PAM50 subtype
    rather than by index, since K-Means indices are not comparable across conditions.}
    \label{tab:top5_both}
    \begin{tabular}{lll}
    \hline
    \textbf{Cluster type} & \textbf{miRNA only} & \textbf{miRNA + clinical} \\
    \hline
    Basal-Like
    & MIR-18A/18A* (1.46) & MIR-18A/18A* (1.43) \\
    & MIR-577/577 (1.41)  & MIR-505/505 (1.34)  \\
    & MIR-505/505 (1.37)  & MIR-577/577 (1.33)  \\
    & MIR-934/934 (1.32)  & MIR-505/505* (1.28) \\
    & MIR-505/505* (1.29) & MIR-224/224* (1.27) \\
    \hline
    Lum.\ A (MIR-197 signature)
    & MIR-197/197 (1.05)  & MIR-197/197 (1.12)  \\
    & MIR-186/186* (0.99) & MIR-324/5P (1.09)   \\
    & MIR-324/5P (0.99)   & MIR-186/186* (1.07) \\
    & MIR-146B/5P (0.97)  & MIR-146B/5P (1.04)  \\
    & MIR-24-1/24 (0.96)  & MIR-590/5P (1.02)   \\
    \hline
    Lum.\ A (MIR-199 signature)
    & MIR-199B/5P (0.89)   & MIR-151/3P (0.81)    \\
    & MIR-151/3P (0.79)    & MIR-374C/374C (0.78) \\
    & MIR-145/145 (0.76)   & MIR-199B/5P (0.77)   \\
    & MIR-374C/374C (0.76) & MIR-3607/3P (0.75)   \\
    & MIR-3130-1/5P (0.75) & MIR-3130-1/5P (0.74) \\
    \hline
    Lum.\ B / HER2 mixed
    & MIR-33B/33B* (0.88) & MIR-33B/33B* (0.88) \\
    & MIR-3677/3P (0.85)  & MIR-3677/3P (0.86)  \\
    & MIR-142/5P (0.81)   & MIR-589/5P (0.80)   \\
    & MIR-326/326 (0.80)  & MIR-142/5P (0.79)   \\
    & MIR-589/5P (0.80)   & MIR-3607/3P (0.79)  \\
    \hline
    \end{tabular}
\end{table}

Several observations emerge from comparing the signatures across conditions.

\paragraph{The Basal-Like signature is highly stable across conditions.}
The Basal-Like cluster produces the strongest feature signal in both conditions, with
MIR-18A/18A* as the top-ranked feature in both ($z = 1.46$ and $z = 1.43$,
respectively). The top-5 features are essentially the same set: MIR-18A/18A*,
MIR-505/505, and MIR-577/577 appear in both rankings with nearly identical z-scores.
This stability is a key positive result: it confirms that the miRNA signature driving
the Basal-Like cluster is robust to the addition of clinical covariates, and that the
slight purity drop observed under miRNA + clinical reflects a small shift in which
samples are geometrically attracted to the Basal centroid, not a change in the centroid's
miRNA character.

\paragraph{One Luminal A cluster is consistently defined by a miR-197-centred signature.}
The Luminal A cluster with higher z-scores is dominated by MIR-197/197, MIR-186/186*,
MIR-324/5P, and MIR-146B/5P in both conditions, with MIR-197/197 ranking first in both.
The strong
correspondence between conditions for this cluster suggests it captures a biologically
robust Luminal A subgroup whose miRNA identity is not perturbed by the addition of
clinical features.

\paragraph{The two Luminal A clusters share no features within each condition.}
Within each condition, the two Luminal A-dominant clusters have completely
non-overlapping top-20 feature lists. In the miRNA-only condition, one is characterised
by the MIR-197/MIR-186 group (z-scores $\approx 1.0$) while the other is defined by
MIR-199B, MIR-151, and MIR-145 (z-scores $\approx 0.8$). This complete divergence in
feature signatures, consistent across both conditions, reinforces the interpretation
that the two Luminal A clusters represent genuinely distinct biological subgroups rather
than an arbitrary partition of the same population.

\paragraph{The Luminal B / HER2-Enriched mixed cluster shows the weakest and most
stable signal.}
Consistent with its mixed composition and lowest overall purity, the Luminal B /
HER2-Enriched cluster exhibits the lowest maximum z-scores of all four clusters in both
conditions ($< 0.90$). Notably, its top features (MIR-33B/33B* and MIR-3677/3P)
rank first and second in both conditions with virtually identical z-scores. This
stability, paradoxically, reflects the fact that the cluster is primarily defined by
what Luminal B and HER2-Enriched samples share in miRNA space, not by any strong
subtype-specific signal.

Taken together, the feature analysis confirms that the miRNA-only condition is superior
both quantitatively (higher external metrics) and interpretively (no negative-coefficient
clusters, cleaner Basal purity). More importantly, the miRNA signatures identified as
cluster-defining are largely preserved when clinical features are added, lending
confidence to their biological relevance. The comparison of these signatures with the
discriminative features from the supervised pipeline is the subject of the following
section.

% ----------------------------------------------------------
\section{Cross-Paradigm Validation: Converging on Subtype-Defining miRNAs}
\label{sec:unsupervised:comparison}

The two analytical paradigms developed in this thesis - supervised classification via
XGBoost and ANOVA, and unsupervised clustering via K-Means - were designed to be
complementary rather than redundant. The supervised approach asks which miRNAs are
discriminative when a model is explicitly trained to separate subtypes; the unsupervised
approach asks which miRNAs define the geometry of the data when no labels are consulted.
A feature that appears in both answers is therefore supported by two independent, 
fundamentally different lines of evidence: it is statistically and informationally 
relevant to a trained classifier, and it is also a structural anchor of the
data's intrinsic organisation. This section formalises that cross-paradigm intersection
and draws the biological conclusions that follow from it.

For each subtype, the doubly-validated panel is defined as the set of miRNAs that appear
in both the XGBoost top-20 gain importance ranking (miRNA-only condition) and the
K-Means top-20 centroid z-score ranking for the cluster dominated by that subtype
(miRNA-only condition). Figure~\ref{fig:venn_diagrams} shows the Venn diagrams of the
feature set overlaps for each subtype, and Table~\ref{tab:cross_paradigm_intersection}
summarises the intersections.

\todo{colocar aqui diagrama de vern ou guardar para o fim?}

% \begin{figure}[t]
%     \centering
%     \begin{subfigure}[b]{0.48\textwidth}
%         \centering
%         \includegraphics[width=\textwidth]{venn_basal.png}
%         \caption{Basal-Like}
%         \label{fig:venn_basal}
%     \end{subfigure}
%     \hfill
%     \begin{subfigure}[b]{0.48\textwidth}
%         \centering
%         \includegraphics[width=\textwidth]{venn_her2.png}
%         \caption{HER2-Enriched}
%         \label{fig:venn_her2}
%     \end{subfigure}

%     \vspace{1em}

%     \begin{subfigure}[b]{0.48\textwidth}
%         \centering
%         \includegraphics[width=\textwidth]{venn_luma.png}
%         \caption{Luminal A}
%         \label{fig:venn_luma}
%     \end{subfigure}
%     \hfill
%     \begin{subfigure}[b]{0.48\textwidth}
%         \centering
%         \includegraphics[width=\textwidth]{venn_lumb.png}
%         \caption{Luminal B}
%         \label{fig:venn_lumb}
%     \end{subfigure}

%     \caption{Venn diagrams of the top-20 feature sets from the supervised (XGBoost gain,
%     miRNA-only) and unsupervised (K-Means centroid z-score, miRNA-only) approaches for
%     each PAM50 subtype. The intersection represents the doubly-validated miRNA panel for
%     each subtype. The size of the intersection relative to the full sets reflects the
%     degree of convergence between the two paradigms.}
%     \label{fig:venn_diagrams}
% \end{figure}

\begin{table}[t]
\centering
\caption{Cross-paradigm miRNA intersection for each PAM50 subtype. Features listed
appear in the top-20 rankings of both XGBoost gain importance and K-Means centroid
z-score under the miRNA-only condition. The corresponding unsupervised cluster and its
purity are indicated for reference.}
\label{tab:cross_paradigm_intersection}
\begin{tabular}{llcl}
\hline
\textbf{Subtype} & \textbf{Unsupervised cluster} & \textbf{Intersection size} &
\textbf{Doubly-validated miRNAs} \\
\hline
Basal-Like  & C1 (72.3\% purity) & 5/20 &
  MIR-577/577, MIR-135B/135B*, \\
& & & MIR-934/934, MIR-505/505, \\
& & & MIR-18A/18A* \\
\hline
HER2-Enriched & C3 (41.3\%, mixed) & $\leq$2/20 & MIR-142/5P, MIR-3150B/3P \\
\hline
Luminal A & C0 (66.2\%) + C2 (63.6\%) & $\leq$2/20 each &
  MIR-326/326 (C0 only) \\
\hline
Luminal B & C3 (41.3\%, mixed) & 1/20 & MIR-3677/3P \\
\hline
\end{tabular}
\end{table}

% ----------------------------------------------------------
\subsection{Subtype-by-Subtype Interpretation}
\label{sec:unsupervised:discussion}

The intersection results paint a coherent and biologically grounded picture of the
information content of miRNA expression data with respect to PAM50 subtype structure.
Rather than producing a uniform result across subtypes, the cross-paradigm analysis
reveals a clear gradient: one subtype is robustly characterised, one is partially so,
and two remain elusive. This gradient is not a failure of the methodology, it is
itself a finding.

\paragraph{Basal-Like: a robust, compact, and convergent miRNA panel.}

Basal-Like is the only subtype for which both analytical paradigms converge onto a
substantial and overlapping feature set. Five miRNAs, \textbf{(MIR-577/577, MIR-135B/135B*,
MIR-934/934, MIR-505/505, and MIR-18A/18A*)}, appear simultaneously in the top-20
XGBoost gain rankings and the top-20 K-Means centroid z-scores for the Basal-dominant
cluster. This is a strong result for several compounding reasons. First, the supervised
classifier achieved perfect classification for Basal-Like ($\text{F1} = 1.00$), and the
ANOVA F-scores for its top features (386 for MIR-934, 358 for MIR-577, 292 for
MIR-135B) are an order of magnitude above those of any other subtype's top features.
Second, the unsupervised Basal-Like cluster is the most geometrically coherent of the
four ($72.3\%$ purity, highest across both conditions), and its centroid z-scores are
the highest in the entire analysis (MIR-18A/18A* at $z = 1.46$). Third, the feature
signatures are stable across experimental conditions: adding clinical covariates does
not materially change the top-ranked miRNAs for this cluster. Taken together, these
converging lines of evidence support the conclusion that the five doubly-validated
miRNAs constitute a compact, robust, and biologically credible panel for Basal-Like
subtype identification. This is consistent with the well-established clinical
distinctiveness of Basal-Like (triple-negative) tumours, which exhibit a markedly
different molecular programme from hormone-receptor-positive subtypes and have been
consistently separable in expression-based studies since the earliest molecular
classifications of breast cancer \cite{perou2000molecular}.

\paragraph{Luminal A: a partially distinctive subtype with intrinsic heterogeneity.}

Luminal A occupies an intermediate position. The supervised approach identifies a
distributed miRNA signature - the gain is spread across $\sim$20 features, with no
single dominant marker (top feature contributes only $\sim$5\% of gain) - and
classification performance is moderate ($\text{F1} = 0.81$). The unsupervised analysis
adds a structural observation that the supervised approach cannot: Luminal A is not a
geometrically cohesive entity in miRNA expression space. It consistently fragments into
two distinct clusters, each dominated by a completely non-overlapping set of miRNAs.
One subgroup is characterised by MIR-197/197, MIR-186/186*, and MIR-324/5P; the other
by MIR-199B/5P, MIR-151/3P, and MIR-145/145. These two feature sets are orthogonal
within each condition, and neither overlaps substantially with the supervised top-20
for Luminal A.

This divergence between the supervised and unsupervised feature sets for Luminal A
should not be interpreted as a contradiction. The supervised model learns boundaries
that maximally separate Luminal A from other subtypes in a multivariate discriminative
setting; the unsupervised model identifies the directions of greatest internal variation
within the cohort. The fact that these two objectives yield different miRNA rankings
for Luminal A is itself informative: it suggests that Luminal A is internally
heterogeneous, containing at least two biologically distinct subpopulations whose
within-group variation is comparable to their between-group signal against other
subtypes. This is consistent with growing evidence in the literature that the Luminal A
category, as defined by PAM50, conflates molecularly distinct entities
\cite{perou2000molecular}. The practical consequence for biomarker discovery is that
a single panel of Luminal A markers is likely to be insufficient; any clinically
deployed panel would need to account for this heterogeneity by either targeting the
shared boundary features identified by the supervised approach, or by embracing the
two-subgroup structure revealed by clustering and defining separate panels for each.

\paragraph{HER2-Enriched: a diffuse signature with no compact discriminative panel.}

HER2-Enriched is the most analytically challenging subtype, and the cross-paradigm
results confirm this systematically. In the supervised analysis, classification
performance was the weakest of the four subtypes ($\text{F1} = 0.50$, with low recall
of $0.33$), the gain profile is the most distributed of all classes (top feature
MIR-30/30A contributes only $\sim$15\% of total gain), and the ANOVA F-scores for its
top features barely exceed 50, confirming the absence of a statistically dominant,
individually discriminative marker. In the unsupervised analysis, HER2-Enriched samples
are never captured by a dedicated cluster: they are consistently co-assigned with
Luminal B samples in a mixed cluster of $41\%$ purity. The near-identical composition
of this mixed cluster across both experimental conditions ($28.6\%$ HER2-Enriched in
miRNA-only; $27.7\%$ in miRNA + clinical) confirms that the inseparability of
HER2-Enriched and Luminal B is not a consequence of any particular feature set, but
an intrinsic property of the miRNA expression space. The cross-paradigm intersection
for HER2-Enriched is consequently minimal (at most 2 features), and these features
(MIR-142/5P, MIR-3150B/3P) are shared with a mixed cluster rather than a
HER2-specific one. The conclusion is unambiguous: \textbf{miRNA expression alone is
insufficient to define a robust discriminative panel for HER2-Enriched tumours.} The
supervised results confirm that the most informative supplementary signal for this
subtype is \texttt{HER2\_STATUS}, a clinical covariate that encodes protein-level
receptor amplification information that miRNA expression does not capture. Any
practical diagnostic panel for HER2-Enriched classification would therefore require
the integration of molecular receptor status data alongside miRNA profiling.

\paragraph{Luminal B: no compact panel, boundary inseparability confirmed.}

Luminal B presents the most challenging scenario of all four subtypes. Supervised
classification performance was the lowest ($\text{F1} = 0.50$ under miRNA-only),
improved moderately with the addition of clinical features ($\text{F1} = 0.64$), and
the gain distribution is the most diffuse across all subtypes (top 20 features account
for only $\sim$50\% of total gain). In the unsupervised analysis, Luminal B shares its
dedicated cluster with HER2-Enriched samples and only contributes $41.3\%$ of that
cluster's composition. The cross-paradigm intersection is limited to a single feature:
MIR-3677/3P, which ranks first in the supervised top-20 for Luminal B and second in
the unsupervised mixed cluster. This single-feature overlap is insufficient to
constitute a discriminative panel. The picture is therefore consistent across both
paradigms: Luminal B lacks a geometrically compact and statistically concentrated
miRNA identity, and its boundary with Luminal A on one side and HER2-Enriched on the
other is diffuse in miRNA expression space. As with HER2-Enriched, the supervised
results suggest that clinical features (particularly \texttt{ER\_STATUS}) carry
discriminative information that miRNA expression alone does not provide. Unlike
HER2-Enriched, however, even the addition of clinical features does not fully resolve
the boundary; the improvement in F1-score from $0.50$ to $0.64$ is meaningful but
leaves substantial room for further misclassification.

% ================================================================
\subsection{Summary of Conclusions}
\label{sec:unsupervised:summary}

The cross-paradigm analysis yields a coherent and internally consistent set of
conclusions that address the central question of this thesis: \textit{which miRNAs
carry subtype-discriminative information, and what do the two analytical paradigms
together tell us about the biological structure of the PAM50 classification?}

The results can be summarised along three axes.

\textbf{In terms of discriminative miRNA panels:} A compact, doubly-validated panel
exists for Basal-Like only, comprising MIR-577/577, MIR-135B/135B*, MIR-934/934,
MIR-505/505, and MIR-18A/18A*. For Luminal A, a panel of candidate features can be
identified from the supervised analysis, but the unsupervised results caution that this
panel describes a boundary rather than a coherent subgroup, and that a two-panel
approach accounting for internal heterogeneity may be more appropriate. For HER2-Enriched
and Luminal B, no compact miRNA-only panel is supported by either paradigm; their
discrimination requires integration of clinical receptor status data.

\textbf{In terms of the data's intrinsic structure:} The unsupervised analysis reveals
that the miRNA expression space of this cohort contains one compact and isolated
subgroup (Basal-Like), one heterogeneous but partially structured region (Luminal A),
and one diffuse region where HER2-Enriched and Luminal B co-occupy indistinct
geometric neighbourhoods. This structure is stable across experimental conditions and
is not an artefact of any particular algorithmic choice.

\textbf{In terms of the value of the unsupervised approach:} The K-Means analysis adds
genuine scientific value beyond what the supervised analysis alone could establish. It
confirms the Basal-Like panel through an entirely independent lens; it reveals the
internal fragmentation of Luminal A that the classifier treats as a single class; and
it places the difficulty of HER2-Enriched and Luminal B classification in a geometric
context, showing that these subtypes do not occupy separable regions of miRNA space
regardless of the algorithm used. Together, the two paradigms provide a more complete
and more trustworthy characterisation of the miRNA landscape of breast cancer subtypes
than either could achieve individually.
